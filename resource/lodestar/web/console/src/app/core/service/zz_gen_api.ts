// Code generated by resourcegeneration. DO NOT EDIT.
import { ApiDescriptor, Client, ClientOptions, createClient, MethodHandle, ResourceHandle } from '@cccteam/resource';
import { Methods, Resources } from './zz_gen_constants';
import { Clients, ClientContacts, Consignments, DistressCalls, FeeByKinds, Hangars, Missions, OpenMissionsBySquadrons, Pilots, PilotCertifications, Refits, RefitTasks, Sectors, Ships, ShipClasses, Sorties, SortieExpenses, Squadrons, SquadronMemberships, Wings, SectorHazardBoards, ServiceLedgers } from './zz_gen_resources';
import { BeginRefit, ClaimMission, CompleteMission, FailFlightTest, FailMission, HailShip, HoldMission, InspectShip, IssueBulletin, LaunchMission, PassFlightTest, ReleaseConsignment, ResumeMission, ScrapShip, StandDownMission, StartFlightTest } from './zz_gen_methods';

/**
 * The fields a client may set when creating Clients. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface ClientsCreate {
  name: string;
  contactName: string;
  contactEmail: string;
  trusted: boolean;
}
/** The fields a client may change on Clients. Keys, server-owned, and immutable fields are absent. */
export interface ClientsPatch {
  name?: string;
  contactName?: string;
  contactEmail?: string;
  trusted?: boolean;
}
/** The primary key of Clients, in route order. */
export type ClientsKey = [id: string];

/**
 * The fields a client may set when creating ClientContacts. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface ClientContactsCreate {
  userId: string;
  clientId: string;
  displayName: string;
}
/** The fields a client may change on ClientContacts. Keys, server-owned, and immutable fields are absent. */
export interface ClientContactsPatch {
  userId?: string;
  clientId?: string;
  displayName?: string;
}
/** The primary key of ClientContacts, in route order. */
export type ClientContactsKey = [id: string];

/**
 * The fields a client may set when creating Consignments. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface ConsignmentsCreate {
  clientId: string;
  bondCode: string;
  description: string;
  mass: number;
  expiresOn: Date;
  releasedAt?: Date;
}
/** The fields a client may change on Consignments. Keys, server-owned, and immutable fields are absent. */
export interface ConsignmentsPatch {
  clientId?: string;
  description?: string;
  mass?: number;
  expiresOn?: Date;
  releasedAt?: Date;
}
/** The primary key of Consignments, in route order. */
export type ConsignmentsKey = [id: string];

/**
 * The fields a client may set when creating DistressCalls. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface DistressCallsCreate {
  summary: string;
  severity: number;
  callerContact?: string;
  transcript?: string;
}
/** The fields a client may change on DistressCalls. Keys, server-owned, and immutable fields are absent. */
export interface DistressCallsPatch {
  summary?: string;
  severity?: number;
  callerContact?: string;
  transcript?: string;
}
/** The primary key of DistressCalls, in route order. */
export type DistressCallsKey = [id: string];

/** The primary key of FeeByKinds, in route order. */
export type FeeByKindsKey = [kindId: string];

/**
 * The fields a client may set when creating Hangars. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface HangarsCreate {
  name: string;
  zone: string;
}
/** The fields a client may change on Hangars. Keys, server-owned, and immutable fields are absent. */
export interface HangarsPatch {
  name?: string;
  zone?: string;
}
/** The primary key of Hangars, in route order. */
export type HangarsKey = [id: string];

/**
 * The fields a client may set when creating Missions. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface MissionsCreate {
  clientId: string;
  kindId: string;
  title: string;
  brief?: string;
  hazard: number;
  fee: number;
  deadline: Date;
  requiredCertId?: string;
  assignedSquadronId?: string;
  notes?: string;
}
/** The fields a client may change on Missions. Keys, server-owned, and immutable fields are absent. */
export interface MissionsPatch {
  clientId?: string;
  kindId?: string;
  title?: string;
  brief?: string;
  hazard?: number;
  fee?: number;
  deadline?: Date;
  requiredCertId?: string;
  assignedSquadronId?: string;
  notes?: string;
}
/** The primary key of Missions, in route order. */
export type MissionsKey = [id: string];

/** The primary key of OpenMissionsBySquadrons, in route order. */
export type OpenMissionsBySquadronsKey = [squadronId: string, sectorId: string];

/**
 * The fields a client may set when creating Pilots. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface PilotsCreate {
  userId: string;
  displayName: string;
  clearance: number;
  feeLimit: number;
}
/** The fields a client may change on Pilots. Keys, server-owned, and immutable fields are absent. */
export interface PilotsPatch {
  userId?: string;
  displayName?: string;
  clearance?: number;
  feeLimit?: number;
}
/** The primary key of Pilots, in route order. */
export type PilotsKey = [id: string];

/**
 * The fields a client may set when creating PilotCertifications. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface PilotCertificationsCreate {
  userId: string;
  certificationId: string;
}
/** The fields a client may change on PilotCertifications. Keys, server-owned, and immutable fields are absent. */
export type PilotCertificationsPatch = Record<never, never>;
/** The primary key of PilotCertifications, in route order. */
export type PilotCertificationsKey = [userId: string, certificationId: string];

/**
 * The fields a client may set when creating Refits. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface RefitsCreate {
  shipId: string;
  estimate?: number;
  notes?: string;
}
/** The fields a client may change on Refits. Keys, server-owned, and immutable fields are absent. */
export interface RefitsPatch {
  shipId?: string;
  estimate?: number;
  notes?: string;
}
/** The primary key of Refits, in route order. */
export type RefitsKey = [id: string];

/**
 * The fields a client may set when creating RefitTasks. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface RefitTasksCreate {
  refitId: string;
  taskNumber: number;
  instructions: string;
  done: boolean;
  notes?: string;
}
/** The fields a client may change on RefitTasks. Keys, server-owned, and immutable fields are absent. */
export interface RefitTasksPatch {
  instructions?: string;
  done?: boolean;
  notes?: string;
}
/** The primary key of RefitTasks, in route order. */
export type RefitTasksKey = [refitId: string, taskNumber: number];

/**
 * The fields a client may set when creating Sectors. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface SectorsCreate {
  id: string;
  name: string;
  region: string;
  established: Date;
}
/** The fields a client may change on Sectors. Keys, server-owned, and immutable fields are absent. */
export interface SectorsPatch {
  name?: string;
  region?: string;
  established?: Date;
}
/** The primary key of Sectors, in route order. */
export type SectorsKey = [id: string];

/**
 * The fields a client may set when creating Ships. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface ShipsCreate {
  hangarId: string;
  classId: string;
  registry: string;
  name: string;
}
/** The fields a client may change on Ships. Keys, server-owned, and immutable fields are absent. */
export interface ShipsPatch {
  hangarId?: string;
  classId?: string;
  name?: string;
}
/** The primary key of Ships, in route order. */
export type ShipsKey = [id: string];

/**
 * The fields a client may set when creating ShipClasses. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface ShipClassesCreate {
  designation: string;
  roleId: string;
  tonnage: number;
  hardened: boolean;
}
/** The fields a client may change on ShipClasses. Keys, server-owned, and immutable fields are absent. */
export interface ShipClassesPatch {
  roleId?: string;
  tonnage?: number;
  hardened?: boolean;
}
/** The primary key of ShipClasses, in route order. */
export type ShipClassesKey = [id: string];

/**
 * The fields a client may set when creating Sorties. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface SortiesCreate {
  missionId: string;
  shipId: string;
  pilotUserId: string;
  launchedAt: Date;
  returnedAt?: Date;
  debrief?: string;
}
/** The fields a client may change on Sorties. Keys, server-owned, and immutable fields are absent. */
export interface SortiesPatch {
  missionId?: string;
  shipId?: string;
  pilotUserId?: string;
  launchedAt?: Date;
  returnedAt?: Date;
  debrief?: string;
}
/** The primary key of Sorties, in route order. */
export type SortiesKey = [id: string];

/**
 * The fields a client may set when creating SortieExpenses. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface SortieExpensesCreate {
  sortieId: string;
  category: string;
  amount: number;
  note?: string;
}
/** The fields a client may change on SortieExpenses. Keys, server-owned, and immutable fields are absent. */
export interface SortieExpensesPatch {
  sortieId?: string;
  category?: string;
  amount?: number;
  note?: string;
}
/** The primary key of SortieExpenses, in route order. */
export type SortieExpensesKey = [id: string];

/**
 * The fields a client may set when creating Squadrons. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface SquadronsCreate {
  wingId: string;
  name: string;
}
/** The fields a client may change on Squadrons. Keys, server-owned, and immutable fields are absent. */
export interface SquadronsPatch {
  wingId?: string;
  name?: string;
}
/** The primary key of Squadrons, in route order. */
export type SquadronsKey = [id: string];

/**
 * The fields a client may set when creating SquadronMemberships. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface SquadronMembershipsCreate {
  squadronId: string;
  userId: string;
}
/** The fields a client may change on SquadronMemberships. Keys, server-owned, and immutable fields are absent. */
export type SquadronMembershipsPatch = Record<never, never>;
/** The primary key of SquadronMemberships, in route order. */
export type SquadronMembershipsKey = [squadronId: string, userId: string];

/**
 * The fields a client may set when creating Wings. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface WingsCreate {
  name: string;
}
/** The fields a client may change on Wings. Keys, server-owned, and immutable fields are absent. */
export interface WingsPatch {
  name?: string;
}
/** The primary key of Wings, in route order. */
export type WingsKey = [id: string];

/** The primary key of SectorHazardBoards, in route order. */
export type SectorHazardBoardsKey = [shipId: string, subsystem: string];

/** The primary key of ServiceLedgers, in route order. */
export type ServiceLedgersKey = [sectorId: string];

/** The generated API as the @cccteam/resource runtime addresses it. Routes carry no API prefix. */
export const apiDescriptor: ApiDescriptor = {
  domainRoute: { segment: 'sectors', param: 'sectorID' },
  consolidatedRoute: 'resources',
  permissionDigestRoute: 'permission-digest',
  userDomainsRoute: 'user-domains',
  resources: {
    [Resources.Clients]: {
      resource: Resources.Clients,
      property: 'clients',
      route: 'clients',
      scope: 'global',
      consolidated: false,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove'],
      patchable: ['name', 'contactName', 'contactEmail', 'trusted'],
    },
    [Resources.ClientContacts]: {
      resource: Resources.ClientContacts,
      property: 'clientContacts',
      route: 'client-contacts',
      scope: 'global',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['userId', 'clientId', 'displayName'],
    },
    [Resources.Consignments]: {
      resource: Resources.Consignments,
      property: 'consignments',
      route: 'consignments',
      scope: 'domain',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['clientId', 'description', 'mass', 'expiresOn', 'releasedAt'],
    },
    [Resources.DistressCalls]: {
      resource: Resources.DistressCalls,
      property: 'distressCalls',
      route: 'distress-calls',
      scope: 'domain',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['summary', 'severity', 'callerContact', 'transcript'],
    },
    [Resources.FeeByKinds]: {
      resource: Resources.FeeByKinds,
      property: 'feeByKinds',
      route: 'fee-by-kinds',
      scope: 'global',
      consolidated: false,
      keys: ['kindId'],
      operations: ['list', 'read'],
    },
    [Resources.Hangars]: {
      resource: Resources.Hangars,
      property: 'hangars',
      route: 'hangars',
      scope: 'domain',
      consolidated: false,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove'],
      patchable: ['name', 'zone'],
    },
    [Resources.Missions]: {
      resource: Resources.Missions,
      property: 'missions',
      route: 'missions',
      scope: 'domain',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['clientId', 'kindId', 'title', 'brief', 'hazard', 'fee', 'deadline', 'requiredCertId', 'assignedSquadronId', 'notes'],
    },
    [Resources.OpenMissionsBySquadrons]: {
      resource: Resources.OpenMissionsBySquadrons,
      property: 'openMissionsBySquadrons',
      route: 'open-missions-by-squadrons',
      scope: 'domain',
      consolidated: false,
      keys: ['squadronId', 'sectorId'],
      operations: ['list', 'read'],
    },
    [Resources.Pilots]: {
      resource: Resources.Pilots,
      property: 'pilots',
      route: 'pilots',
      scope: 'global',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['userId', 'displayName', 'clearance', 'feeLimit'],
    },
    [Resources.PilotCertifications]: {
      resource: Resources.PilotCertifications,
      property: 'pilotCertifications',
      route: 'pilot-certifications',
      scope: 'global',
      consolidated: true,
      keys: ['userId', 'certificationId'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: [],
    },
    [Resources.Refits]: {
      resource: Resources.Refits,
      property: 'refits',
      route: 'refits',
      scope: 'domain',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['shipId', 'estimate', 'notes'],
    },
    [Resources.RefitTasks]: {
      resource: Resources.RefitTasks,
      property: 'refitTasks',
      route: 'refit-tasks',
      scope: 'domain',
      consolidated: true,
      keys: ['refitId', 'taskNumber'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['instructions', 'done', 'notes'],
    },
    [Resources.Sectors]: {
      resource: Resources.Sectors,
      property: 'sectors',
      route: 'sectors',
      scope: 'global',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['name', 'region', 'established'],
    },
    [Resources.Ships]: {
      resource: Resources.Ships,
      property: 'ships',
      route: 'ships',
      scope: 'domain',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['hangarId', 'classId', 'name'],
    },
    [Resources.ShipClasses]: {
      resource: Resources.ShipClasses,
      property: 'shipClasses',
      route: 'ship-classes',
      scope: 'global',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['roleId', 'tonnage', 'hardened'],
    },
    [Resources.Sorties]: {
      resource: Resources.Sorties,
      property: 'sorties',
      route: 'sorties',
      scope: 'domain',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['missionId', 'shipId', 'pilotUserId', 'launchedAt', 'returnedAt', 'debrief'],
    },
    [Resources.SortieExpenses]: {
      resource: Resources.SortieExpenses,
      property: 'sortieExpenses',
      route: 'sortie-expenses',
      scope: 'domain',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['sortieId', 'category', 'amount', 'note'],
    },
    [Resources.Squadrons]: {
      resource: Resources.Squadrons,
      property: 'squadrons',
      route: 'squadrons',
      scope: 'domain',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['wingId', 'name'],
    },
    [Resources.SquadronMemberships]: {
      resource: Resources.SquadronMemberships,
      property: 'squadronMemberships',
      route: 'squadron-memberships',
      scope: 'domain',
      consolidated: true,
      keys: ['squadronId', 'userId'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: [],
    },
    [Resources.Wings]: {
      resource: Resources.Wings,
      property: 'wings',
      route: 'wings',
      scope: 'domain',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['name'],
    },
    [Resources.SectorHazardBoards]: {
      resource: Resources.SectorHazardBoards,
      property: 'sectorHazardBoards',
      route: 'sector-hazard-boards',
      scope: 'domain',
      consolidated: false,
      keys: ['shipId', 'subsystem'],
      operations: ['list', 'read'],
    },
    [Resources.ServiceLedgers]: {
      resource: Resources.ServiceLedgers,
      property: 'serviceLedgers',
      route: 'service-ledgers',
      scope: 'global',
      consolidated: false,
      keys: ['sectorId'],
      operations: ['list'],
    },
  },
  methods: {
    [Methods.BeginRefit]: { method: Methods.BeginRefit, property: 'beginRefit', route: 'begin-refit', scope: 'domain' },
    [Methods.ClaimMission]: { method: Methods.ClaimMission, property: 'claimMission', route: 'claim-mission', scope: 'domain' },
    [Methods.CompleteMission]: { method: Methods.CompleteMission, property: 'completeMission', route: 'complete-mission', scope: 'domain' },
    [Methods.FailFlightTest]: { method: Methods.FailFlightTest, property: 'failFlightTest', route: 'fail-flight-test', scope: 'domain' },
    [Methods.FailMission]: { method: Methods.FailMission, property: 'failMission', route: 'fail-mission', scope: 'domain' },
    [Methods.HailShip]: { method: Methods.HailShip, property: 'hailShip', route: 'hail-ship', scope: 'domain' },
    [Methods.HoldMission]: { method: Methods.HoldMission, property: 'holdMission', route: 'hold-mission', scope: 'domain' },
    [Methods.InspectShip]: { method: Methods.InspectShip, property: 'inspectShip', route: 'inspect-ship', scope: 'domain' },
    [Methods.IssueBulletin]: { method: Methods.IssueBulletin, property: 'issueBulletin', route: 'issue-bulletin', scope: 'global' },
    [Methods.LaunchMission]: { method: Methods.LaunchMission, property: 'launchMission', route: 'launch-mission', scope: 'domain' },
    [Methods.PassFlightTest]: { method: Methods.PassFlightTest, property: 'passFlightTest', route: 'pass-flight-test', scope: 'domain' },
    [Methods.ReleaseConsignment]: { method: Methods.ReleaseConsignment, property: 'releaseConsignment', route: 'release-consignment', scope: 'domain' },
    [Methods.ResumeMission]: { method: Methods.ResumeMission, property: 'resumeMission', route: 'resume-mission', scope: 'domain' },
    [Methods.ScrapShip]: { method: Methods.ScrapShip, property: 'scrapShip', route: 'scrap-ship', scope: 'domain' },
    [Methods.StandDownMission]: { method: Methods.StandDownMission, property: 'standDownMission', route: 'stand-down-mission', scope: 'domain' },
    [Methods.StartFlightTest]: { method: Methods.StartFlightTest, property: 'startFlightTest', route: 'start-flight-test', scope: 'domain' },
  },
};

/** Handles for the global scope, available on the client root. */
export interface GlobalApi {
  clients: ResourceHandle<Clients, ClientsKey, 'list' | 'read' | 'create' | 'patch' | 'remove', ClientsCreate, ClientsPatch>;
  clientContacts: ResourceHandle<ClientContacts, ClientContactsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', ClientContactsCreate, ClientContactsPatch>;
  feeByKinds: ResourceHandle<FeeByKinds, FeeByKindsKey, 'list' | 'read'>;
  pilots: ResourceHandle<Pilots, PilotsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', PilotsCreate, PilotsPatch>;
  pilotCertifications: ResourceHandle<PilotCertifications, PilotCertificationsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', PilotCertificationsCreate, PilotCertificationsPatch>;
  sectors: ResourceHandle<Sectors, SectorsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', SectorsCreate, SectorsPatch>;
  shipClasses: ResourceHandle<ShipClasses, ShipClassesKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', ShipClassesCreate, ShipClassesPatch>;
  serviceLedgers: ResourceHandle<ServiceLedgers, ServiceLedgersKey, 'list'>;
  issueBulletin: MethodHandle<IssueBulletin>;
}

/** Handles for one tenant partition, available on client.domain(...). */
export interface DomainApi {
  consignments: ResourceHandle<Consignments, ConsignmentsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', ConsignmentsCreate, ConsignmentsPatch>;
  distressCalls: ResourceHandle<DistressCalls, DistressCallsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', DistressCallsCreate, DistressCallsPatch>;
  hangars: ResourceHandle<Hangars, HangarsKey, 'list' | 'read' | 'create' | 'patch' | 'remove', HangarsCreate, HangarsPatch>;
  missions: ResourceHandle<Missions, MissionsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', MissionsCreate, MissionsPatch>;
  openMissionsBySquadrons: ResourceHandle<OpenMissionsBySquadrons, OpenMissionsBySquadronsKey, 'list' | 'read'>;
  refits: ResourceHandle<Refits, RefitsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', RefitsCreate, RefitsPatch>;
  refitTasks: ResourceHandle<RefitTasks, RefitTasksKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', RefitTasksCreate, RefitTasksPatch>;
  ships: ResourceHandle<Ships, ShipsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', ShipsCreate, ShipsPatch>;
  sorties: ResourceHandle<Sorties, SortiesKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', SortiesCreate, SortiesPatch>;
  sortieExpenses: ResourceHandle<SortieExpenses, SortieExpensesKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', SortieExpensesCreate, SortieExpensesPatch>;
  squadrons: ResourceHandle<Squadrons, SquadronsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', SquadronsCreate, SquadronsPatch>;
  squadronMemberships: ResourceHandle<SquadronMemberships, SquadronMembershipsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', SquadronMembershipsCreate, SquadronMembershipsPatch>;
  wings: ResourceHandle<Wings, WingsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', WingsCreate, WingsPatch>;
  sectorHazardBoards: ResourceHandle<SectorHazardBoards, SectorHazardBoardsKey, 'list' | 'read'>;
  beginRefit: MethodHandle<BeginRefit>;
  claimMission: MethodHandle<ClaimMission>;
  completeMission: MethodHandle<CompleteMission>;
  failFlightTest: MethodHandle<FailFlightTest>;
  failMission: MethodHandle<FailMission>;
  hailShip: MethodHandle<HailShip>;
  holdMission: MethodHandle<HoldMission>;
  inspectShip: MethodHandle<InspectShip>;
  launchMission: MethodHandle<LaunchMission>;
  passFlightTest: MethodHandle<PassFlightTest>;
  releaseConsignment: MethodHandle<ReleaseConsignment>;
  resumeMission: MethodHandle<ResumeMission>;
  scrapShip: MethodHandle<ScrapShip>;
  standDownMission: MethodHandle<StandDownMission>;
  startFlightTest: MethodHandle<StartFlightTest>;
}

export type Api = Client<GlobalApi, DomainApi>;

/** Creates the typed client. baseUrl is the API prefix (for example '/api'). */
export function createApi(options: ClientOptions): Api {
  return createClient<GlobalApi, DomainApi>(apiDescriptor, options);
}
