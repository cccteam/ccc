// Code generated by resourcegeneration. DO NOT EDIT.
// Source: pkg/resources

package app

import (
	"net/http"
	"time"

	"cloud.google.com/go/civil"
	"github.com/cccteam/ccc"
	"github.com/cccteam/ccc/accesstypes"
	"github.com/cccteam/ccc/resource"
	"github.com/cccteam/ccc/resource/lodestar/pkg/resources"
	"github.com/cccteam/ccc/resource/lodestar/pkg/router"
	"github.com/cccteam/ccc/tracer"
	"github.com/cccteam/httpio"
)

func (a *App) Consignments() http.HandlerFunc {
	type consignment struct {
		ID          ccc.UUID   `json:"id"          index:"true"        perm:"-"`
		SectorID    string     `json:"sectorId"    index:"true"`
		ClientID    ccc.UUID   `json:"clientId"    index:"true"`
		BondCode    string     `json:"bondCode"    index:"true"`
		Description string     `json:"description"`
		Mass        float64    `json:"mass"        allow_filter:"true"`
		ExpiresOn   civil.Date `json:"expiresOn"`
		ReleasedAt  *time.Time `json:"releasedAt"`
	}

	type response []map[string]any

	decoder := NewQueryDecoder[resources.Consignment, consignment](accesstypes.List)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewConsignmentQueryFromQuerySet(querySet)

		resp := response{}
		for row, err := range res.List(ctx, a.ResourceClient()) {
			if err != nil {
				return httpio.NewEncoder(w).ClientMessage(ctx, err)
			}
			rec := (*consignment)(&row.Data)
			rmap := make(map[string]any)
			for _, field := range querySet.Fields() {
				switch string(field) {
				case "ID":
					if !row.Masked("id") {
						rmap["id"] = rec.ID
					}
				case "SectorID":
					if !row.Masked("sectorId") {
						rmap["sectorId"] = rec.SectorID
					}
				case "ClientID":
					if !row.Masked("clientId") {
						rmap["clientId"] = rec.ClientID
					}
				case "BondCode":
					if !row.Masked("bondCode") {
						rmap["bondCode"] = rec.BondCode
					}
				case "Description":
					if !row.Masked("description") {
						rmap["description"] = rec.Description
					}
				case "Mass":
					if !row.Masked("mass") {
						rmap["mass"] = rec.Mass
					}
				case "ExpiresOn":
					if !row.Masked("expiresOn") {
						rmap["expiresOn"] = rec.ExpiresOn
					}
				case "ReleasedAt":
					if !row.Masked("releasedAt") {
						rmap["releasedAt"] = rec.ReleasedAt
					}
				}
			}
			if capabilities := row.Capabilities(); capabilities != nil {
				rmap[resource.CapabilitiesProperty] = capabilities
			}
			resp = append(resp, rmap)
		}

		return httpio.NewEncoder(w).Ok(resp)
	})
}

func (a *App) Consignment() http.HandlerFunc {
	type response struct {
		ID          ccc.UUID   `json:"id"          index:"true" perm:"-"`
		SectorID    string     `json:"sectorId"`
		ClientID    ccc.UUID   `json:"clientId"`
		BondCode    string     `json:"bondCode"    index:"true"`
		Description string     `json:"description"`
		Mass        float64    `json:"mass"`
		ExpiresOn   civil.Date `json:"expiresOn"`
		ReleasedAt  *time.Time `json:"releasedAt"`
	}

	decoder := NewQueryDecoder[resources.Consignment, response](accesstypes.Read)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		id := httpio.Param[ccc.UUID](r, router.ConsignmentID)

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewConsignmentQueryFromQuerySet(querySet).SetID(id)

		row, err := res.Read(ctx, a.ResourceClient())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}
		rec := (*response)(&row.Data)
		rmap := make(map[string]any)
		for _, field := range querySet.Fields() {
			switch string(field) {
			case "ID":
				if !row.Masked("id") {
					rmap["id"] = rec.ID
				}
			case "SectorID":
				if !row.Masked("sectorId") {
					rmap["sectorId"] = rec.SectorID
				}
			case "ClientID":
				if !row.Masked("clientId") {
					rmap["clientId"] = rec.ClientID
				}
			case "BondCode":
				if !row.Masked("bondCode") {
					rmap["bondCode"] = rec.BondCode
				}
			case "Description":
				if !row.Masked("description") {
					rmap["description"] = rec.Description
				}
			case "Mass":
				if !row.Masked("mass") {
					rmap["mass"] = rec.Mass
				}
			case "ExpiresOn":
				if !row.Masked("expiresOn") {
					rmap["expiresOn"] = rec.ExpiresOn
				}
			case "ReleasedAt":
				if !row.Masked("releasedAt") {
					rmap["releasedAt"] = rec.ReleasedAt
				}
			}
		}
		if capabilities := row.Capabilities(); capabilities != nil {
			rmap[resource.CapabilitiesProperty] = capabilities
		}

		return httpio.NewEncoder(w).Ok(rmap)
	})
}
