// Code generated by resourcegeneration. DO NOT EDIT.
// Source: pkg/resources

package app

import (
	"net/http"
	"time"

	"github.com/cccteam/ccc"
	"github.com/cccteam/ccc/accesstypes"
	"github.com/cccteam/ccc/resource"
	"github.com/cccteam/ccc/resource/lodestar/pkg/resources"
	"github.com/cccteam/ccc/resource/lodestar/pkg/router"
	"github.com/cccteam/ccc/tracer"
	"github.com/cccteam/httpio"
)

func (a *App) Sorties() http.HandlerFunc {
	type sortie struct {
		ID          ccc.UUID   `json:"id"          index:"true" perm:"-"`
		MissionID   ccc.UUID   `json:"missionId"   index:"true"`
		ShipID      ccc.UUID   `json:"shipId"      index:"true"`
		PilotUserID string     `json:"pilotUserId"`
		LaunchedAt  time.Time  `json:"launchedAt"`
		ReturnedAt  *time.Time `json:"returnedAt"`
		Debrief     *string    `json:"debrief"`
	}

	type response []map[string]any

	decoder := NewQueryDecoder[resources.Sortie, sortie](accesstypes.List)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewSortieQueryFromQuerySet(querySet)

		resp := response{}
		for row, err := range res.List(ctx, a.ResourceClient()) {
			if err != nil {
				return httpio.NewEncoder(w).ClientMessage(ctx, err)
			}
			rec := (*sortie)(&row.Data)
			rmap := make(map[string]any)
			for _, field := range querySet.Fields() {
				switch string(field) {
				case "ID":
					if !row.Masked("id") {
						rmap["id"] = rec.ID
					}
				case "MissionID":
					if !row.Masked("missionId") {
						rmap["missionId"] = rec.MissionID
					}
				case "ShipID":
					if !row.Masked("shipId") {
						rmap["shipId"] = rec.ShipID
					}
				case "PilotUserID":
					if !row.Masked("pilotUserId") {
						rmap["pilotUserId"] = rec.PilotUserID
					}
				case "LaunchedAt":
					if !row.Masked("launchedAt") {
						rmap["launchedAt"] = rec.LaunchedAt
					}
				case "ReturnedAt":
					if !row.Masked("returnedAt") {
						rmap["returnedAt"] = rec.ReturnedAt
					}
				case "Debrief":
					if !row.Masked("debrief") {
						rmap["debrief"] = rec.Debrief
					}
				}
			}
			if capabilities := row.Capabilities(); capabilities != nil {
				rmap[resource.CapabilitiesProperty] = capabilities
			}
			resp = append(resp, rmap)
		}

		return httpio.NewEncoder(w).Ok(resp)
	})
}

func (a *App) Sortie() http.HandlerFunc {
	type response struct {
		ID          ccc.UUID   `json:"id"          index:"true" perm:"-"`
		MissionID   ccc.UUID   `json:"missionId"`
		ShipID      ccc.UUID   `json:"shipId"`
		PilotUserID string     `json:"pilotUserId"`
		LaunchedAt  time.Time  `json:"launchedAt"`
		ReturnedAt  *time.Time `json:"returnedAt"`
		Debrief     *string    `json:"debrief"`
	}

	decoder := NewQueryDecoder[resources.Sortie, response](accesstypes.Read)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		id := httpio.Param[ccc.UUID](r, router.SortieID)

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewSortieQueryFromQuerySet(querySet).SetID(id)

		row, err := res.Read(ctx, a.ResourceClient())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}
		rec := (*response)(&row.Data)
		rmap := make(map[string]any)
		for _, field := range querySet.Fields() {
			switch string(field) {
			case "ID":
				if !row.Masked("id") {
					rmap["id"] = rec.ID
				}
			case "MissionID":
				if !row.Masked("missionId") {
					rmap["missionId"] = rec.MissionID
				}
			case "ShipID":
				if !row.Masked("shipId") {
					rmap["shipId"] = rec.ShipID
				}
			case "PilotUserID":
				if !row.Masked("pilotUserId") {
					rmap["pilotUserId"] = rec.PilotUserID
				}
			case "LaunchedAt":
				if !row.Masked("launchedAt") {
					rmap["launchedAt"] = rec.LaunchedAt
				}
			case "ReturnedAt":
				if !row.Masked("returnedAt") {
					rmap["returnedAt"] = rec.ReturnedAt
				}
			case "Debrief":
				if !row.Masked("debrief") {
					rmap["debrief"] = rec.Debrief
				}
			}
		}
		if capabilities := row.Capabilities(); capabilities != nil {
			rmap[resource.CapabilitiesProperty] = capabilities
		}

		return httpio.NewEncoder(w).Ok(rmap)
	})
}
