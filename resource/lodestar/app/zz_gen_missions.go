// Code generated by resourcegeneration. DO NOT EDIT.
// Source: pkg/resources

package app

import (
	"net/http"
	"time"

	"github.com/cccteam/ccc"
	"github.com/cccteam/ccc/accesstypes"
	"github.com/cccteam/ccc/resource"
	"github.com/cccteam/ccc/resource/lodestar/pkg/resources"
	"github.com/cccteam/ccc/resource/lodestar/pkg/router"
	"github.com/cccteam/ccc/tracer"
	"github.com/cccteam/httpio"
	"github.com/shopspring/decimal"
)

func (a *App) Missions() http.HandlerFunc {
	type mission struct {
		ID                 ccc.UUID            `json:"id"                 index:"true" perm:"-"`
		SectorID           string              `json:"sectorId"           index:"true"`
		ClientID           ccc.UUID            `json:"clientId"           index:"true"`
		KindID             string              `json:"kindId"             index:"true"`
		Title              string              `json:"title"`
		Brief              *string             `json:"brief"`
		Hazard             int64               `json:"hazard"`
		Fee                decimal.Decimal     `json:"fee"`
		Deadline           time.Time           `json:"deadline"`
		RequiredCertID     *string             `json:"requiredCertId"     index:"true"`
		BookedBy           string              `json:"bookedBy"`
		AssignedSquadronID ccc.NullUUID        `json:"assignedSquadronId" index:"true"`
		StatusID           string              `json:"statusId"           index:"true"`
		Notes              *string             `json:"notes"`
		Settlement         decimal.NullDecimal `json:"settlement"`
	}

	type response []map[string]any

	decoder := NewQueryDecoder[resources.Mission, mission](accesstypes.List)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewMissionQueryFromQuerySet(querySet)

		resp := response{}
		for row, err := range res.List(ctx, a.ResourceClient()) {
			if err != nil {
				return httpio.NewEncoder(w).ClientMessage(ctx, err)
			}
			rec := (*mission)(&row.Data)
			rmap := make(map[string]any)
			for _, field := range querySet.Fields() {
				switch string(field) {
				case "ID":
					if !row.Masked("id") {
						rmap["id"] = rec.ID
					}
				case "SectorID":
					if !row.Masked("sectorId") {
						rmap["sectorId"] = rec.SectorID
					}
				case "ClientID":
					if !row.Masked("clientId") {
						rmap["clientId"] = rec.ClientID
					}
				case "KindID":
					if !row.Masked("kindId") {
						rmap["kindId"] = rec.KindID
					}
				case "Title":
					if !row.Masked("title") {
						rmap["title"] = rec.Title
					}
				case "Brief":
					if !row.Masked("brief") {
						rmap["brief"] = rec.Brief
					}
				case "Hazard":
					if !row.Masked("hazard") {
						rmap["hazard"] = rec.Hazard
					}
				case "Fee":
					if !row.Masked("fee") {
						rmap["fee"] = rec.Fee
					}
				case "Deadline":
					if !row.Masked("deadline") {
						rmap["deadline"] = rec.Deadline
					}
				case "RequiredCertID":
					if !row.Masked("requiredCertId") {
						rmap["requiredCertId"] = rec.RequiredCertID
					}
				case "BookedBy":
					if !row.Masked("bookedBy") {
						rmap["bookedBy"] = rec.BookedBy
					}
				case "AssignedSquadronID":
					if !row.Masked("assignedSquadronId") {
						rmap["assignedSquadronId"] = rec.AssignedSquadronID
					}
				case "StatusID":
					if !row.Masked("statusId") {
						rmap["statusId"] = rec.StatusID
					}
				case "Notes":
					if !row.Masked("notes") {
						rmap["notes"] = rec.Notes
					}
				case "Settlement":
					if !row.Masked("settlement") {
						rmap["settlement"] = rec.Settlement
					}
				}
			}
			if capabilities := row.Capabilities(); capabilities != nil {
				rmap[resource.CapabilitiesProperty] = capabilities
			}
			resp = append(resp, rmap)
		}

		return httpio.NewEncoder(w).Ok(resp)
	})
}

func (a *App) Mission() http.HandlerFunc {
	type response struct {
		ID                 ccc.UUID            `json:"id"                 index:"true" perm:"-"`
		SectorID           string              `json:"sectorId"`
		ClientID           ccc.UUID            `json:"clientId"`
		KindID             string              `json:"kindId"`
		Title              string              `json:"title"`
		Brief              *string             `json:"brief"`
		Hazard             int64               `json:"hazard"`
		Fee                decimal.Decimal     `json:"fee"`
		Deadline           time.Time           `json:"deadline"`
		RequiredCertID     *string             `json:"requiredCertId"`
		BookedBy           string              `json:"bookedBy"`
		AssignedSquadronID ccc.NullUUID        `json:"assignedSquadronId"`
		StatusID           string              `json:"statusId"`
		Notes              *string             `json:"notes"`
		Settlement         decimal.NullDecimal `json:"settlement"`
	}

	decoder := NewQueryDecoder[resources.Mission, response](accesstypes.Read)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		id := httpio.Param[ccc.UUID](r, router.MissionID)

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewMissionQueryFromQuerySet(querySet).SetID(id)

		row, err := res.Read(ctx, a.ResourceClient())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}
		rec := (*response)(&row.Data)
		rmap := make(map[string]any)
		for _, field := range querySet.Fields() {
			switch string(field) {
			case "ID":
				if !row.Masked("id") {
					rmap["id"] = rec.ID
				}
			case "SectorID":
				if !row.Masked("sectorId") {
					rmap["sectorId"] = rec.SectorID
				}
			case "ClientID":
				if !row.Masked("clientId") {
					rmap["clientId"] = rec.ClientID
				}
			case "KindID":
				if !row.Masked("kindId") {
					rmap["kindId"] = rec.KindID
				}
			case "Title":
				if !row.Masked("title") {
					rmap["title"] = rec.Title
				}
			case "Brief":
				if !row.Masked("brief") {
					rmap["brief"] = rec.Brief
				}
			case "Hazard":
				if !row.Masked("hazard") {
					rmap["hazard"] = rec.Hazard
				}
			case "Fee":
				if !row.Masked("fee") {
					rmap["fee"] = rec.Fee
				}
			case "Deadline":
				if !row.Masked("deadline") {
					rmap["deadline"] = rec.Deadline
				}
			case "RequiredCertID":
				if !row.Masked("requiredCertId") {
					rmap["requiredCertId"] = rec.RequiredCertID
				}
			case "BookedBy":
				if !row.Masked("bookedBy") {
					rmap["bookedBy"] = rec.BookedBy
				}
			case "AssignedSquadronID":
				if !row.Masked("assignedSquadronId") {
					rmap["assignedSquadronId"] = rec.AssignedSquadronID
				}
			case "StatusID":
				if !row.Masked("statusId") {
					rmap["statusId"] = rec.StatusID
				}
			case "Notes":
				if !row.Masked("notes") {
					rmap["notes"] = rec.Notes
				}
			case "Settlement":
				if !row.Masked("settlement") {
					rmap["settlement"] = rec.Settlement
				}
			}
		}
		if capabilities := row.Capabilities(); capabilities != nil {
			rmap[resource.CapabilitiesProperty] = capabilities
		}

		return httpio.NewEncoder(w).Ok(rmap)
	})
}
