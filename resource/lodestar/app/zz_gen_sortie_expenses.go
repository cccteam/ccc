// Code generated by resourcegeneration. DO NOT EDIT.
// Source: pkg/resources

package app

import (
	"net/http"

	"github.com/cccteam/ccc"
	"github.com/cccteam/ccc/accesstypes"
	"github.com/cccteam/ccc/resource"
	"github.com/cccteam/ccc/resource/lodestar/pkg/resources"
	"github.com/cccteam/ccc/resource/lodestar/pkg/router"
	"github.com/cccteam/ccc/tracer"
	"github.com/cccteam/httpio"
	"github.com/shopspring/decimal"
)

func (a *App) SortieExpenses() http.HandlerFunc {
	type sortieExpense struct {
		ID       ccc.UUID        `json:"id"       index:"true" perm:"-"`
		SortieID ccc.UUID        `json:"sortieId" index:"true"`
		Category string          `json:"category"`
		Amount   decimal.Decimal `json:"amount"`
		Note     *string         `json:"note"`
	}

	type response []map[string]any

	decoder := NewQueryDecoder[resources.SortieExpense, sortieExpense](accesstypes.List)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewSortieExpenseQueryFromQuerySet(querySet)

		resp := response{}
		for row, err := range res.List(ctx, a.ResourceClient()) {
			if err != nil {
				return httpio.NewEncoder(w).ClientMessage(ctx, err)
			}
			rec := (*sortieExpense)(&row.Data)
			rmap := make(map[string]any)
			for _, field := range querySet.Fields() {
				switch string(field) {
				case "ID":
					if !row.Masked("id") {
						rmap["id"] = rec.ID
					}
				case "SortieID":
					if !row.Masked("sortieId") {
						rmap["sortieId"] = rec.SortieID
					}
				case "Category":
					if !row.Masked("category") {
						rmap["category"] = rec.Category
					}
				case "Amount":
					if !row.Masked("amount") {
						rmap["amount"] = rec.Amount
					}
				case "Note":
					if !row.Masked("note") {
						rmap["note"] = rec.Note
					}
				}
			}
			if capabilities := row.Capabilities(); capabilities != nil {
				rmap[resource.CapabilitiesProperty] = capabilities
			}
			resp = append(resp, rmap)
		}

		return httpio.NewEncoder(w).Ok(resp)
	})
}

func (a *App) SortieExpense() http.HandlerFunc {
	type response struct {
		ID       ccc.UUID        `json:"id"       index:"true" perm:"-"`
		SortieID ccc.UUID        `json:"sortieId"`
		Category string          `json:"category"`
		Amount   decimal.Decimal `json:"amount"`
		Note     *string         `json:"note"`
	}

	decoder := NewQueryDecoder[resources.SortieExpense, response](accesstypes.Read)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		id := httpio.Param[ccc.UUID](r, router.SortieExpenseID)

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewSortieExpenseQueryFromQuerySet(querySet).SetID(id)

		row, err := res.Read(ctx, a.ResourceClient())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}
		rec := (*response)(&row.Data)
		rmap := make(map[string]any)
		for _, field := range querySet.Fields() {
			switch string(field) {
			case "ID":
				if !row.Masked("id") {
					rmap["id"] = rec.ID
				}
			case "SortieID":
				if !row.Masked("sortieId") {
					rmap["sortieId"] = rec.SortieID
				}
			case "Category":
				if !row.Masked("category") {
					rmap["category"] = rec.Category
				}
			case "Amount":
				if !row.Masked("amount") {
					rmap["amount"] = rec.Amount
				}
			case "Note":
				if !row.Masked("note") {
					rmap["note"] = rec.Note
				}
			}
		}
		if capabilities := row.Capabilities(); capabilities != nil {
			rmap[resource.CapabilitiesProperty] = capabilities
		}

		return httpio.NewEncoder(w).Ok(rmap)
	})
}
