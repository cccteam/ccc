// Code generated by resourcegeneration. DO NOT EDIT.
// Source: pkg/resources

package app

import (
	"net/http"

	"github.com/cccteam/ccc"
	"github.com/cccteam/ccc/accesstypes"
	"github.com/cccteam/ccc/resource/starport/pkg/resources"
	"github.com/cccteam/ccc/resource/starport/pkg/router"
	"github.com/cccteam/ccc/tracer"
	"github.com/cccteam/httpio"
)

func (a *App) CargoManifests() http.HandlerFunc {
	type cargoManifest struct {
		ShipID        ccc.UUID `json:"shipId"        index:"true" perm:"-"`
		LineNumber    int64    `json:"lineNumber"    index:"true" perm:"-"`
		Details       string   `json:"details"`
		Quantity      int64    `json:"quantity"`
		DeclaredValue int64    `json:"declaredValue"`
	}

	type response []map[string]any

	decoder := NewQueryDecoder[resources.CargoManifest, cargoManifest](accesstypes.List)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.GlobalScope())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewCargoManifestQueryFromQuerySet(querySet)

		resp := response{}
		for row, err := range res.List(ctx, a.ResourceClient()) {
			if err != nil {
				return httpio.NewEncoder(w).ClientMessage(ctx, err)
			}
			rec := (*cargoManifest)(&row.Data)
			rmap := make(map[string]any)
			for _, field := range querySet.Fields() {
				switch string(field) {
				case "ShipID":
					rmap["shipId"] = rec.ShipID
				case "LineNumber":
					rmap["lineNumber"] = rec.LineNumber
				case "Details":
					rmap["details"] = rec.Details
				case "Quantity":
					rmap["quantity"] = rec.Quantity
				case "DeclaredValue":
					rmap["declaredValue"] = rec.DeclaredValue
				}
			}
			resp = append(resp, rmap)
		}

		return httpio.NewEncoder(w).Ok(resp)
	})
}

func (a *App) CargoManifest() http.HandlerFunc {
	type response struct {
		ShipID        ccc.UUID `json:"shipId"        index:"true" perm:"-"`
		LineNumber    int64    `json:"lineNumber"    index:"true" perm:"-"`
		Details       string   `json:"details"`
		Quantity      int64    `json:"quantity"`
		DeclaredValue int64    `json:"declaredValue"`
	}

	decoder := NewQueryDecoder[resources.CargoManifest, response](accesstypes.Read)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		shipID := httpio.Param[ccc.UUID](r, router.CargoManifestShipID)
		lineNumber := httpio.Param[int64](r, router.CargoManifestLineNumber)

		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.GlobalScope())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewCargoManifestQueryFromQuerySet(querySet).SetShipID(shipID).SetLineNumber(lineNumber)

		row, err := res.Read(ctx, a.ResourceClient())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}
		rec := (*response)(&row.Data)
		rmap := make(map[string]any)
		for _, field := range querySet.Fields() {
			switch string(field) {
			case "ShipID":
				rmap["shipId"] = rec.ShipID
			case "LineNumber":
				rmap["lineNumber"] = rec.LineNumber
			case "Details":
				rmap["details"] = rec.Details
			case "Quantity":
				rmap["quantity"] = rec.Quantity
			case "DeclaredValue":
				rmap["declaredValue"] = rec.DeclaredValue
			}
		}

		return httpio.NewEncoder(w).Ok(rmap)
	})
}
