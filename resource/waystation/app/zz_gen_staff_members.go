// Code generated by resourcegeneration. DO NOT EDIT.
// Source: pkg/resources

package app

import (
	"net/http"

	"github.com/cccteam/ccc"
	"github.com/cccteam/ccc/accesstypes"
	"github.com/cccteam/ccc/resource/waystation/pkg/resources"
	"github.com/cccteam/ccc/resource/waystation/pkg/router"
	"github.com/cccteam/ccc/tracer"
	"github.com/cccteam/httpio"
	"github.com/shopspring/decimal"
)

func (a *App) StaffMembers() http.HandlerFunc {
	type staffMember struct {
		ID               ccc.UUID        `json:"id"               index:"true" perm:"-"`
		UserID           string          `json:"userId"           index:"true"`
		DisplayName      string          `json:"displayName"`
		ApprovalLimit    decimal.Decimal `json:"approvalLimit"`
		HomeWaystationID *string         `json:"homeWaystationId" index:"true"`
	}

	type response []map[string]any

	decoder := NewQueryDecoder[resources.StaffMember, staffMember](accesstypes.List)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.GlobalScope())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewStaffMemberQueryFromQuerySet(querySet)

		resp := response{}
		for row, err := range res.List(ctx, a.ResourceClient()) {
			if err != nil {
				return httpio.NewEncoder(w).ClientMessage(ctx, err)
			}
			rec := (*staffMember)(&row.Data)
			rmap := make(map[string]any)
			for _, field := range querySet.Fields() {
				switch string(field) {
				case "ID":
					if !row.Masked("id") {
						rmap["id"] = rec.ID
					}
				case "UserID":
					if !row.Masked("userId") {
						rmap["userId"] = rec.UserID
					}
				case "DisplayName":
					if !row.Masked("displayName") {
						rmap["displayName"] = rec.DisplayName
					}
				case "ApprovalLimit":
					if !row.Masked("approvalLimit") {
						rmap["approvalLimit"] = rec.ApprovalLimit
					}
				case "HomeWaystationID":
					if !row.Masked("homeWaystationId") {
						rmap["homeWaystationId"] = rec.HomeWaystationID
					}
				}
			}
			resp = append(resp, rmap)
		}

		return httpio.NewEncoder(w).Ok(resp)
	})
}

func (a *App) StaffMember() http.HandlerFunc {
	type response struct {
		ID               ccc.UUID        `json:"id"               index:"true" perm:"-"`
		UserID           string          `json:"userId"           index:"true"`
		DisplayName      string          `json:"displayName"`
		ApprovalLimit    decimal.Decimal `json:"approvalLimit"`
		HomeWaystationID *string         `json:"homeWaystationId"`
	}

	decoder := NewQueryDecoder[resources.StaffMember, response](accesstypes.Read)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		id := httpio.Param[ccc.UUID](r, router.StaffMemberID)

		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.GlobalScope())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewStaffMemberQueryFromQuerySet(querySet).SetID(id)

		row, err := res.Read(ctx, a.ResourceClient())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}
		rec := (*response)(&row.Data)
		rmap := make(map[string]any)
		for _, field := range querySet.Fields() {
			switch string(field) {
			case "ID":
				if !row.Masked("id") {
					rmap["id"] = rec.ID
				}
			case "UserID":
				if !row.Masked("userId") {
					rmap["userId"] = rec.UserID
				}
			case "DisplayName":
				if !row.Masked("displayName") {
					rmap["displayName"] = rec.DisplayName
				}
			case "ApprovalLimit":
				if !row.Masked("approvalLimit") {
					rmap["approvalLimit"] = rec.ApprovalLimit
				}
			case "HomeWaystationID":
				if !row.Masked("homeWaystationId") {
					rmap["homeWaystationId"] = rec.HomeWaystationID
				}
			}
		}

		return httpio.NewEncoder(w).Ok(rmap)
	})
}
