// Code generated by resourcegeneration. DO NOT EDIT.
// Source: pkg/resources

package app

import (
	"net/http"

	"github.com/cccteam/ccc"
	"github.com/cccteam/ccc/accesstypes"
	"github.com/cccteam/ccc/resource/waystation/pkg/resources"
	"github.com/cccteam/ccc/resource/waystation/pkg/router"
	"github.com/cccteam/ccc/tracer"
	"github.com/cccteam/httpio"
)

func (a *App) IncidentReports() http.HandlerFunc {
	type incidentReport struct {
		ID              ccc.UUID `json:"id"              index:"true" perm:"-"`
		WaystationID    string   `json:"waystationId"    index:"true"`
		Summary         string   `json:"summary"`
		Severity        int64    `json:"severity"`
		ReporterContact string   `json:"reporterContact" pii:"true"`
		RawStatement    *string  `json:"-"`
		CaseNumber      string   `json:"caseNumber"      index:"true"`
	}

	type response []map[string]any

	decoder := NewQueryDecoder[resources.IncidentReport, incidentReport](accesstypes.List)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewIncidentReportQueryFromQuerySet(querySet)

		resp := response{}
		for row, err := range res.List(ctx, a.ResourceClient()) {
			if err != nil {
				return httpio.NewEncoder(w).ClientMessage(ctx, err)
			}
			rec := (*incidentReport)(&row.Data)
			rmap := make(map[string]any)
			for _, field := range querySet.Fields() {
				switch string(field) {
				case "ID":
					if !row.Masked("id") {
						rmap["id"] = rec.ID
					}
				case "WaystationID":
					if !row.Masked("waystationId") {
						rmap["waystationId"] = rec.WaystationID
					}
				case "Summary":
					if !row.Masked("summary") {
						rmap["summary"] = rec.Summary
					}
				case "Severity":
					if !row.Masked("severity") {
						rmap["severity"] = rec.Severity
					}
				case "ReporterContact":
					if !row.Masked("reporterContact") {
						rmap["reporterContact"] = rec.ReporterContact
					}
				case "CaseNumber":
					if !row.Masked("caseNumber") {
						rmap["caseNumber"] = rec.CaseNumber
					}
				}
			}
			resp = append(resp, rmap)
		}

		return httpio.NewEncoder(w).Ok(resp)
	})
}

func (a *App) IncidentReport() http.HandlerFunc {
	type response struct {
		ID              ccc.UUID `json:"id"              index:"true" perm:"-"`
		WaystationID    string   `json:"waystationId"`
		Summary         string   `json:"summary"`
		Severity        int64    `json:"severity"`
		ReporterContact string   `json:"reporterContact" pii:"true"`
		RawStatement    *string  `json:"-"`
		CaseNumber      string   `json:"caseNumber"      index:"true"`
	}

	decoder := NewQueryDecoder[resources.IncidentReport, response](accesstypes.Read)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		id := httpio.Param[ccc.UUID](r, router.IncidentReportID)

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewIncidentReportQueryFromQuerySet(querySet).SetID(id)

		row, err := res.Read(ctx, a.ResourceClient())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}
		rec := (*response)(&row.Data)
		rmap := make(map[string]any)
		for _, field := range querySet.Fields() {
			switch string(field) {
			case "ID":
				if !row.Masked("id") {
					rmap["id"] = rec.ID
				}
			case "WaystationID":
				if !row.Masked("waystationId") {
					rmap["waystationId"] = rec.WaystationID
				}
			case "Summary":
				if !row.Masked("summary") {
					rmap["summary"] = rec.Summary
				}
			case "Severity":
				if !row.Masked("severity") {
					rmap["severity"] = rec.Severity
				}
			case "ReporterContact":
				if !row.Masked("reporterContact") {
					rmap["reporterContact"] = rec.ReporterContact
				}
			case "CaseNumber":
				if !row.Masked("caseNumber") {
					rmap["caseNumber"] = rec.CaseNumber
				}
			}
		}

		return httpio.NewEncoder(w).Ok(rmap)
	})
}
