// Code generated by resourcegeneration. DO NOT EDIT.
// Source: pkg/resources

package app

import (
	"net/http"

	"cloud.google.com/go/civil"
	"github.com/cccteam/ccc"
	"github.com/cccteam/ccc/accesstypes"
	"github.com/cccteam/ccc/resource"
	"github.com/cccteam/ccc/resource/waystation/pkg/resources"
	"github.com/cccteam/ccc/resource/waystation/pkg/router"
	"github.com/cccteam/ccc/tracer"
	"github.com/cccteam/httpio"
	"github.com/shopspring/decimal"
)

func (a *App) Requisitions() http.HandlerFunc {
	type requisition struct {
		ID            ccc.UUID        `json:"id"            index:"true" perm:"-"`
		WaystationID  string          `json:"waystationId"  index:"true"`
		RequestedBy   string          `json:"requestedBy"`
		Justification *string         `json:"justification"`
		NeededBy      civil.Date      `json:"neededBy"`
		TotalCost     decimal.Decimal `json:"totalCost"`
		StatusID      string          `json:"statusId"      index:"true"`
	}

	type response []map[string]any

	decoder := NewQueryDecoder[resources.Requisition, requisition](accesstypes.List)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewRequisitionQueryFromQuerySet(querySet)

		resp := response{}
		for row, err := range res.List(ctx, a.ResourceClient()) {
			if err != nil {
				return httpio.NewEncoder(w).ClientMessage(ctx, err)
			}
			rec := (*requisition)(&row.Data)
			rmap := make(map[string]any)
			for _, field := range querySet.Fields() {
				switch string(field) {
				case "ID":
					if !row.Masked("id") {
						rmap["id"] = rec.ID
					}
				case "WaystationID":
					if !row.Masked("waystationId") {
						rmap["waystationId"] = rec.WaystationID
					}
				case "RequestedBy":
					if !row.Masked("requestedBy") {
						rmap["requestedBy"] = rec.RequestedBy
					}
				case "Justification":
					if !row.Masked("justification") {
						rmap["justification"] = rec.Justification
					}
				case "NeededBy":
					if !row.Masked("neededBy") {
						rmap["neededBy"] = rec.NeededBy
					}
				case "TotalCost":
					if !row.Masked("totalCost") {
						rmap["totalCost"] = rec.TotalCost
					}
				case "StatusID":
					if !row.Masked("statusId") {
						rmap["statusId"] = rec.StatusID
					}
				}
			}
			if capabilities := row.Capabilities(); capabilities != nil {
				rmap[resource.CapabilitiesProperty] = capabilities
			}
			resp = append(resp, rmap)
		}

		return httpio.NewEncoder(w).Ok(resp)
	})
}

func (a *App) Requisition() http.HandlerFunc {
	type response struct {
		ID            ccc.UUID        `json:"id"            index:"true" perm:"-"`
		WaystationID  string          `json:"waystationId"`
		RequestedBy   string          `json:"requestedBy"`
		Justification *string         `json:"justification"`
		NeededBy      civil.Date      `json:"neededBy"`
		TotalCost     decimal.Decimal `json:"totalCost"`
		StatusID      string          `json:"statusId"`
	}

	decoder := NewQueryDecoder[resources.Requisition, response](accesstypes.Read)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		id := httpio.Param[ccc.UUID](r, router.RequisitionID)

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewRequisitionQueryFromQuerySet(querySet).SetID(id)

		row, err := res.Read(ctx, a.ResourceClient())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}
		rec := (*response)(&row.Data)
		rmap := make(map[string]any)
		for _, field := range querySet.Fields() {
			switch string(field) {
			case "ID":
				if !row.Masked("id") {
					rmap["id"] = rec.ID
				}
			case "WaystationID":
				if !row.Masked("waystationId") {
					rmap["waystationId"] = rec.WaystationID
				}
			case "RequestedBy":
				if !row.Masked("requestedBy") {
					rmap["requestedBy"] = rec.RequestedBy
				}
			case "Justification":
				if !row.Masked("justification") {
					rmap["justification"] = rec.Justification
				}
			case "NeededBy":
				if !row.Masked("neededBy") {
					rmap["neededBy"] = rec.NeededBy
				}
			case "TotalCost":
				if !row.Masked("totalCost") {
					rmap["totalCost"] = rec.TotalCost
				}
			case "StatusID":
				if !row.Masked("statusId") {
					rmap["statusId"] = rec.StatusID
				}
			}
		}
		if capabilities := row.Capabilities(); capabilities != nil {
			rmap[resource.CapabilitiesProperty] = capabilities
		}

		return httpio.NewEncoder(w).Ok(rmap)
	})
}
