// Code generated by resourcegeneration. DO NOT EDIT.
// Source: pkg/resources

package app

import (
	"net/http"
	"time"

	"github.com/cccteam/ccc"
	"github.com/cccteam/ccc/accesstypes"
	"github.com/cccteam/ccc/resource/waystation/pkg/resources"
	"github.com/cccteam/ccc/resource/waystation/pkg/router"
	"github.com/cccteam/ccc/tracer"
	"github.com/cccteam/httpio"
)

func (a *App) WorkOrders() http.HandlerFunc {
	type workOrder struct {
		ID             ccc.UUID     `json:"id"             index:"true" perm:"-"`
		WaystationID   string       `json:"waystationId"   index:"true"`
		AssetID        ccc.UUID     `json:"assetId"        index:"true"`
		Title          string       `json:"title"`
		Summary        *string      `json:"summary"`
		Priority       int64        `json:"priority"`
		StatusID       string       `json:"statusId"       index:"true"`
		CreatedBy      string       `json:"createdBy"`
		AssignedTeamID ccc.NullUUID `json:"assignedTeamId" index:"true"`
		DueAt          *time.Time   `json:"dueAt"`
	}

	type response []map[string]any

	decoder := NewQueryDecoder[resources.WorkOrder, workOrder](accesstypes.List)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewWorkOrderQueryFromQuerySet(querySet)

		resp := response{}
		for row, err := range res.List(ctx, a.ResourceClient()) {
			if err != nil {
				return httpio.NewEncoder(w).ClientMessage(ctx, err)
			}
			rec := (*workOrder)(&row.Data)
			rmap := make(map[string]any)
			for _, field := range querySet.Fields() {
				switch string(field) {
				case "ID":
					if !row.Masked("id") {
						rmap["id"] = rec.ID
					}
				case "WaystationID":
					if !row.Masked("waystationId") {
						rmap["waystationId"] = rec.WaystationID
					}
				case "AssetID":
					if !row.Masked("assetId") {
						rmap["assetId"] = rec.AssetID
					}
				case "Title":
					if !row.Masked("title") {
						rmap["title"] = rec.Title
					}
				case "Summary":
					if !row.Masked("summary") {
						rmap["summary"] = rec.Summary
					}
				case "Priority":
					if !row.Masked("priority") {
						rmap["priority"] = rec.Priority
					}
				case "StatusID":
					if !row.Masked("statusId") {
						rmap["statusId"] = rec.StatusID
					}
				case "CreatedBy":
					if !row.Masked("createdBy") {
						rmap["createdBy"] = rec.CreatedBy
					}
				case "AssignedTeamID":
					if !row.Masked("assignedTeamId") {
						rmap["assignedTeamId"] = rec.AssignedTeamID
					}
				case "DueAt":
					if !row.Masked("dueAt") {
						rmap["dueAt"] = rec.DueAt
					}
				}
			}
			resp = append(resp, rmap)
		}

		return httpio.NewEncoder(w).Ok(resp)
	})
}

func (a *App) WorkOrder() http.HandlerFunc {
	type response struct {
		ID             ccc.UUID     `json:"id"             index:"true" perm:"-"`
		WaystationID   string       `json:"waystationId"`
		AssetID        ccc.UUID     `json:"assetId"`
		Title          string       `json:"title"`
		Summary        *string      `json:"summary"`
		Priority       int64        `json:"priority"`
		StatusID       string       `json:"statusId"`
		CreatedBy      string       `json:"createdBy"`
		AssignedTeamID ccc.NullUUID `json:"assignedTeamId"`
		DueAt          *time.Time   `json:"dueAt"`
	}

	decoder := NewQueryDecoder[resources.WorkOrder, response](accesstypes.Read)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		id := httpio.Param[ccc.UUID](r, router.WorkOrderID)

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewWorkOrderQueryFromQuerySet(querySet).SetID(id)

		row, err := res.Read(ctx, a.ResourceClient())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}
		rec := (*response)(&row.Data)
		rmap := make(map[string]any)
		for _, field := range querySet.Fields() {
			switch string(field) {
			case "ID":
				if !row.Masked("id") {
					rmap["id"] = rec.ID
				}
			case "WaystationID":
				if !row.Masked("waystationId") {
					rmap["waystationId"] = rec.WaystationID
				}
			case "AssetID":
				if !row.Masked("assetId") {
					rmap["assetId"] = rec.AssetID
				}
			case "Title":
				if !row.Masked("title") {
					rmap["title"] = rec.Title
				}
			case "Summary":
				if !row.Masked("summary") {
					rmap["summary"] = rec.Summary
				}
			case "Priority":
				if !row.Masked("priority") {
					rmap["priority"] = rec.Priority
				}
			case "StatusID":
				if !row.Masked("statusId") {
					rmap["statusId"] = rec.StatusID
				}
			case "CreatedBy":
				if !row.Masked("createdBy") {
					rmap["createdBy"] = rec.CreatedBy
				}
			case "AssignedTeamID":
				if !row.Masked("assignedTeamId") {
					rmap["assignedTeamId"] = rec.AssignedTeamID
				}
			case "DueAt":
				if !row.Masked("dueAt") {
					rmap["dueAt"] = rec.DueAt
				}
			}
		}

		return httpio.NewEncoder(w).Ok(rmap)
	})
}
