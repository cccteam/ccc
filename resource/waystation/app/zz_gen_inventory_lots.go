// Code generated by resourcegeneration. DO NOT EDIT.
// Source: pkg/resources

package app

import (
	"net/http"

	"cloud.google.com/go/civil"
	"github.com/cccteam/ccc"
	"github.com/cccteam/ccc/accesstypes"
	"github.com/cccteam/ccc/resource"
	"github.com/cccteam/ccc/resource/waystation/pkg/resources"
	"github.com/cccteam/ccc/resource/waystation/pkg/router"
	"github.com/cccteam/ccc/tracer"
	"github.com/cccteam/httpio"
)

func (a *App) InventoryLots() http.HandlerFunc {
	type inventoryLot struct {
		ID            ccc.UUID    `json:"id"            index:"true"        perm:"-"`
		WaystationID  string      `json:"waystationId"  index:"true"`
		CatalogItemID ccc.UUID    `json:"catalogItemId" index:"true"`
		Quantity      int64       `json:"quantity"      allow_filter:"true"`
		ExpiresOn     *civil.Date `json:"expiresOn"`
		BinLocation   string      `json:"binLocation"   index:"true"`
	}

	type response []map[string]any

	decoder := NewQueryDecoder[resources.InventoryLot, inventoryLot](accesstypes.List)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewInventoryLotQueryFromQuerySet(querySet)

		resp := response{}
		for row, err := range res.List(ctx, a.ResourceClient()) {
			if err != nil {
				return httpio.NewEncoder(w).ClientMessage(ctx, err)
			}
			rec := (*inventoryLot)(&row.Data)
			rmap := make(map[string]any)
			for _, field := range querySet.Fields() {
				switch string(field) {
				case "ID":
					if !row.Masked("id") {
						rmap["id"] = rec.ID
					}
				case "WaystationID":
					if !row.Masked("waystationId") {
						rmap["waystationId"] = rec.WaystationID
					}
				case "CatalogItemID":
					if !row.Masked("catalogItemId") {
						rmap["catalogItemId"] = rec.CatalogItemID
					}
				case "Quantity":
					if !row.Masked("quantity") {
						rmap["quantity"] = rec.Quantity
					}
				case "ExpiresOn":
					if !row.Masked("expiresOn") {
						rmap["expiresOn"] = rec.ExpiresOn
					}
				case "BinLocation":
					if !row.Masked("binLocation") {
						rmap["binLocation"] = rec.BinLocation
					}
				}
			}
			if capabilities := row.Capabilities(); capabilities != nil {
				rmap[resource.CapabilitiesProperty] = capabilities
			}
			resp = append(resp, rmap)
		}

		return httpio.NewEncoder(w).Ok(resp)
	})
}

func (a *App) InventoryLot() http.HandlerFunc {
	type response struct {
		ID            ccc.UUID    `json:"id"            index:"true" perm:"-"`
		WaystationID  string      `json:"waystationId"`
		CatalogItemID ccc.UUID    `json:"catalogItemId"`
		Quantity      int64       `json:"quantity"`
		ExpiresOn     *civil.Date `json:"expiresOn"`
		BinLocation   string      `json:"binLocation"`
	}

	decoder := NewQueryDecoder[resources.InventoryLot, response](accesstypes.Read)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		id := httpio.Param[ccc.UUID](r, router.InventoryLotID)

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewInventoryLotQueryFromQuerySet(querySet).SetID(id)

		row, err := res.Read(ctx, a.ResourceClient())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}
		rec := (*response)(&row.Data)
		rmap := make(map[string]any)
		for _, field := range querySet.Fields() {
			switch string(field) {
			case "ID":
				if !row.Masked("id") {
					rmap["id"] = rec.ID
				}
			case "WaystationID":
				if !row.Masked("waystationId") {
					rmap["waystationId"] = rec.WaystationID
				}
			case "CatalogItemID":
				if !row.Masked("catalogItemId") {
					rmap["catalogItemId"] = rec.CatalogItemID
				}
			case "Quantity":
				if !row.Masked("quantity") {
					rmap["quantity"] = rec.Quantity
				}
			case "ExpiresOn":
				if !row.Masked("expiresOn") {
					rmap["expiresOn"] = rec.ExpiresOn
				}
			case "BinLocation":
				if !row.Masked("binLocation") {
					rmap["binLocation"] = rec.BinLocation
				}
			}
		}
		if capabilities := row.Capabilities(); capabilities != nil {
			rmap[resource.CapabilitiesProperty] = capabilities
		}

		return httpio.NewEncoder(w).Ok(rmap)
	})
}
