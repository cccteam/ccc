// Code generated by resourcegeneration. DO NOT EDIT.
// Source: pkg/resources

package app

import (
	"net/http"

	"github.com/cccteam/ccc"
	"github.com/cccteam/ccc/accesstypes"
	"github.com/cccteam/ccc/resource"
	"github.com/cccteam/ccc/resource/waystation/pkg/resources"
	"github.com/cccteam/ccc/resource/waystation/pkg/router"
	"github.com/cccteam/ccc/tracer"
	"github.com/cccteam/httpio"
	"github.com/shopspring/decimal"
)

func (a *App) RequisitionLines() http.HandlerFunc {
	type requisitionLine struct {
		RequisitionID    ccc.UUID        `json:"requisitionId"    index:"true" perm:"-"`
		LineNumber       int64           `json:"lineNumber"       index:"true" perm:"-"`
		CatalogItemID    ccc.UUID        `json:"catalogItemId"    index:"true"`
		Quantity         int64           `json:"quantity"`
		UnitCostSnapshot decimal.Decimal `json:"unitCostSnapshot"`
	}

	type response []map[string]any

	decoder := NewQueryDecoder[resources.RequisitionLine, requisitionLine](accesstypes.List)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewRequisitionLineQueryFromQuerySet(querySet)

		resp := response{}
		for row, err := range res.List(ctx, a.ResourceClient()) {
			if err != nil {
				return httpio.NewEncoder(w).ClientMessage(ctx, err)
			}
			rec := (*requisitionLine)(&row.Data)
			rmap := make(map[string]any)
			for _, field := range querySet.Fields() {
				switch string(field) {
				case "RequisitionID":
					if !row.Masked("requisitionId") {
						rmap["requisitionId"] = rec.RequisitionID
					}
				case "LineNumber":
					if !row.Masked("lineNumber") {
						rmap["lineNumber"] = rec.LineNumber
					}
				case "CatalogItemID":
					if !row.Masked("catalogItemId") {
						rmap["catalogItemId"] = rec.CatalogItemID
					}
				case "Quantity":
					if !row.Masked("quantity") {
						rmap["quantity"] = rec.Quantity
					}
				case "UnitCostSnapshot":
					if !row.Masked("unitCostSnapshot") {
						rmap["unitCostSnapshot"] = rec.UnitCostSnapshot
					}
				}
			}
			if capabilities := row.Capabilities(); capabilities != nil {
				rmap[resource.CapabilitiesProperty] = capabilities
			}
			resp = append(resp, rmap)
		}

		return httpio.NewEncoder(w).Ok(resp)
	})
}

func (a *App) RequisitionLine() http.HandlerFunc {
	type response struct {
		RequisitionID    ccc.UUID        `json:"requisitionId"    index:"true" perm:"-"`
		LineNumber       int64           `json:"lineNumber"       index:"true" perm:"-"`
		CatalogItemID    ccc.UUID        `json:"catalogItemId"`
		Quantity         int64           `json:"quantity"`
		UnitCostSnapshot decimal.Decimal `json:"unitCostSnapshot"`
	}

	decoder := NewQueryDecoder[resources.RequisitionLine, response](accesstypes.Read)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		requisitionID := httpio.Param[ccc.UUID](r, router.RequisitionLineRequisitionID)
		lineNumber := httpio.Param[int64](r, router.RequisitionLineLineNumber)

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewRequisitionLineQueryFromQuerySet(querySet).SetRequisitionID(requisitionID).SetLineNumber(lineNumber)

		row, err := res.Read(ctx, a.ResourceClient())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}
		rec := (*response)(&row.Data)
		rmap := make(map[string]any)
		for _, field := range querySet.Fields() {
			switch string(field) {
			case "RequisitionID":
				if !row.Masked("requisitionId") {
					rmap["requisitionId"] = rec.RequisitionID
				}
			case "LineNumber":
				if !row.Masked("lineNumber") {
					rmap["lineNumber"] = rec.LineNumber
				}
			case "CatalogItemID":
				if !row.Masked("catalogItemId") {
					rmap["catalogItemId"] = rec.CatalogItemID
				}
			case "Quantity":
				if !row.Masked("quantity") {
					rmap["quantity"] = rec.Quantity
				}
			case "UnitCostSnapshot":
				if !row.Masked("unitCostSnapshot") {
					rmap["unitCostSnapshot"] = rec.UnitCostSnapshot
				}
			}
		}
		if capabilities := row.Capabilities(); capabilities != nil {
			rmap[resource.CapabilitiesProperty] = capabilities
		}

		return httpio.NewEncoder(w).Ok(rmap)
	})
}
