// Code generated by resourcegeneration. DO NOT EDIT.
// Source: pkg/resources

package app

import (
	"net/http"

	"github.com/cccteam/ccc"
	"github.com/cccteam/ccc/accesstypes"
	"github.com/cccteam/ccc/resource/waystation/pkg/resources"
	"github.com/cccteam/ccc/resource/waystation/pkg/router"
	"github.com/cccteam/ccc/tracer"
	"github.com/cccteam/httpio"
	"github.com/shopspring/decimal"
)

func (a *App) CatalogItems() http.HandlerFunc {
	type catalogItem struct {
		ID         ccc.UUID        `json:"id"         index:"true" perm:"-"`
		Sku        string          `json:"sku"        index:"true"`
		Name       string          `json:"name"`
		CategoryID string          `json:"categoryId" index:"true"`
		UnitCost   decimal.Decimal `json:"unitCost"`
		Hazardous  bool            `json:"hazardous"`
	}

	type response []map[string]any

	decoder := NewQueryDecoder[resources.CatalogItem, catalogItem](accesstypes.List)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.GlobalScope())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewCatalogItemQueryFromQuerySet(querySet)

		resp := response{}
		for row, err := range res.List(ctx, a.ResourceClient()) {
			if err != nil {
				return httpio.NewEncoder(w).ClientMessage(ctx, err)
			}
			rec := (*catalogItem)(&row.Data)
			rmap := make(map[string]any)
			for _, field := range querySet.Fields() {
				switch string(field) {
				case "ID":
					if !row.Masked("id") {
						rmap["id"] = rec.ID
					}
				case "Sku":
					if !row.Masked("sku") {
						rmap["sku"] = rec.Sku
					}
				case "Name":
					if !row.Masked("name") {
						rmap["name"] = rec.Name
					}
				case "CategoryID":
					if !row.Masked("categoryId") {
						rmap["categoryId"] = rec.CategoryID
					}
				case "UnitCost":
					if !row.Masked("unitCost") {
						rmap["unitCost"] = rec.UnitCost
					}
				case "Hazardous":
					if !row.Masked("hazardous") {
						rmap["hazardous"] = rec.Hazardous
					}
				}
			}
			resp = append(resp, rmap)
		}

		return httpio.NewEncoder(w).Ok(resp)
	})
}

func (a *App) CatalogItem() http.HandlerFunc {
	type response struct {
		ID         ccc.UUID        `json:"id"         index:"true" perm:"-"`
		Sku        string          `json:"sku"        index:"true"`
		Name       string          `json:"name"`
		CategoryID string          `json:"categoryId"`
		UnitCost   decimal.Decimal `json:"unitCost"`
		Hazardous  bool            `json:"hazardous"`
	}

	decoder := NewQueryDecoder[resources.CatalogItem, response](accesstypes.Read)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		id := httpio.Param[ccc.UUID](r, router.CatalogItemID)

		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.GlobalScope())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewCatalogItemQueryFromQuerySet(querySet).SetID(id)

		row, err := res.Read(ctx, a.ResourceClient())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}
		rec := (*response)(&row.Data)
		rmap := make(map[string]any)
		for _, field := range querySet.Fields() {
			switch string(field) {
			case "ID":
				if !row.Masked("id") {
					rmap["id"] = rec.ID
				}
			case "Sku":
				if !row.Masked("sku") {
					rmap["sku"] = rec.Sku
				}
			case "Name":
				if !row.Masked("name") {
					rmap["name"] = rec.Name
				}
			case "CategoryID":
				if !row.Masked("categoryId") {
					rmap["categoryId"] = rec.CategoryID
				}
			case "UnitCost":
				if !row.Masked("unitCost") {
					rmap["unitCost"] = rec.UnitCost
				}
			case "Hazardous":
				if !row.Masked("hazardous") {
					rmap["hazardous"] = rec.Hazardous
				}
			}
		}

		return httpio.NewEncoder(w).Ok(rmap)
	})
}
