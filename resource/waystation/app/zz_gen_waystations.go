// Code generated by resourcegeneration. DO NOT EDIT.
// Source: pkg/resources

package app

import (
	"net/http"

	"cloud.google.com/go/civil"
	"github.com/cccteam/ccc/accesstypes"
	"github.com/cccteam/ccc/resource/waystation/pkg/resources"
	"github.com/cccteam/ccc/resource/waystation/pkg/router"
	"github.com/cccteam/ccc/tracer"
	"github.com/cccteam/httpio"
)

func (a *App) Waystations() http.HandlerFunc {
	type waystation struct {
		ID           string     `json:"id"           index:"true" perm:"-"`
		Name         string     `json:"name"         index:"true"`
		OrbitBand    string     `json:"orbitBand"`
		Commissioned civil.Date `json:"commissioned"`
	}

	type response []map[string]any

	decoder := NewQueryDecoder[resources.Waystation, waystation](accesstypes.List)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.GlobalScope())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewWaystationQueryFromQuerySet(querySet)

		resp := response{}
		for row, err := range res.List(ctx, a.ResourceClient()) {
			if err != nil {
				return httpio.NewEncoder(w).ClientMessage(ctx, err)
			}
			rec := (*waystation)(&row.Data)
			rmap := make(map[string]any)
			for _, field := range querySet.Fields() {
				switch string(field) {
				case "ID":
					if !row.Masked("id") {
						rmap["id"] = rec.ID
					}
				case "Name":
					if !row.Masked("name") {
						rmap["name"] = rec.Name
					}
				case "OrbitBand":
					if !row.Masked("orbitBand") {
						rmap["orbitBand"] = rec.OrbitBand
					}
				case "Commissioned":
					if !row.Masked("commissioned") {
						rmap["commissioned"] = rec.Commissioned
					}
				}
			}
			resp = append(resp, rmap)
		}

		return httpio.NewEncoder(w).Ok(resp)
	})
}

func (a *App) Waystation() http.HandlerFunc {
	type response struct {
		ID           string     `json:"id"           index:"true" perm:"-"`
		Name         string     `json:"name"         index:"true"`
		OrbitBand    string     `json:"orbitBand"`
		Commissioned civil.Date `json:"commissioned"`
	}

	decoder := NewQueryDecoder[resources.Waystation, response](accesstypes.Read)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		id := httpio.Param[string](r, router.WaystationID)

		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.GlobalScope())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewWaystationQueryFromQuerySet(querySet).SetID(id)

		row, err := res.Read(ctx, a.ResourceClient())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}
		rec := (*response)(&row.Data)
		rmap := make(map[string]any)
		for _, field := range querySet.Fields() {
			switch string(field) {
			case "ID":
				if !row.Masked("id") {
					rmap["id"] = rec.ID
				}
			case "Name":
				if !row.Masked("name") {
					rmap["name"] = rec.Name
				}
			case "OrbitBand":
				if !row.Masked("orbitBand") {
					rmap["orbitBand"] = rec.OrbitBand
				}
			case "Commissioned":
				if !row.Masked("commissioned") {
					rmap["commissioned"] = rec.Commissioned
				}
			}
		}

		return httpio.NewEncoder(w).Ok(rmap)
	})
}
