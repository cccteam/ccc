// Code generated by resourcegeneration. DO NOT EDIT.
// Source: pkg/resources

package app

import (
	"net/http"
	"time"

	"github.com/cccteam/ccc"
	"github.com/cccteam/ccc/accesstypes"
	"github.com/cccteam/ccc/resource/waystation/pkg/resources"
	"github.com/cccteam/ccc/resource/waystation/pkg/router"
	"github.com/cccteam/ccc/tracer"
	"github.com/cccteam/httpio"
)

func (a *App) Shipments() http.HandlerFunc {
	type shipment struct {
		ID           ccc.UUID   `json:"id"           index:"true" perm:"-"`
		WaystationID string     `json:"waystationId" index:"true"`
		SupplierID   ccc.UUID   `json:"supplierId"   index:"true"`
		ManifestCode string     `json:"manifestCode" index:"true"`
		ArrivedAt    *time.Time `json:"arrivedAt"`
	}

	type response []map[string]any

	decoder := NewQueryDecoder[resources.Shipment, shipment](accesstypes.List)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewShipmentQueryFromQuerySet(querySet)

		resp := response{}
		for row, err := range res.List(ctx, a.ResourceClient()) {
			if err != nil {
				return httpio.NewEncoder(w).ClientMessage(ctx, err)
			}
			rec := (*shipment)(&row.Data)
			rmap := make(map[string]any)
			for _, field := range querySet.Fields() {
				switch string(field) {
				case "ID":
					if !row.Masked("id") {
						rmap["id"] = rec.ID
					}
				case "WaystationID":
					if !row.Masked("waystationId") {
						rmap["waystationId"] = rec.WaystationID
					}
				case "SupplierID":
					if !row.Masked("supplierId") {
						rmap["supplierId"] = rec.SupplierID
					}
				case "ManifestCode":
					if !row.Masked("manifestCode") {
						rmap["manifestCode"] = rec.ManifestCode
					}
				case "ArrivedAt":
					if !row.Masked("arrivedAt") {
						rmap["arrivedAt"] = rec.ArrivedAt
					}
				}
			}
			resp = append(resp, rmap)
		}

		return httpio.NewEncoder(w).Ok(resp)
	})
}

func (a *App) Shipment() http.HandlerFunc {
	type response struct {
		ID           ccc.UUID   `json:"id"           index:"true" perm:"-"`
		WaystationID string     `json:"waystationId"`
		SupplierID   ccc.UUID   `json:"supplierId"`
		ManifestCode string     `json:"manifestCode" index:"true"`
		ArrivedAt    *time.Time `json:"arrivedAt"`
	}

	decoder := NewQueryDecoder[resources.Shipment, response](accesstypes.Read)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		id := httpio.Param[ccc.UUID](r, router.ShipmentID)

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewShipmentQueryFromQuerySet(querySet).SetID(id)

		row, err := res.Read(ctx, a.ResourceClient())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}
		rec := (*response)(&row.Data)
		rmap := make(map[string]any)
		for _, field := range querySet.Fields() {
			switch string(field) {
			case "ID":
				if !row.Masked("id") {
					rmap["id"] = rec.ID
				}
			case "WaystationID":
				if !row.Masked("waystationId") {
					rmap["waystationId"] = rec.WaystationID
				}
			case "SupplierID":
				if !row.Masked("supplierId") {
					rmap["supplierId"] = rec.SupplierID
				}
			case "ManifestCode":
				if !row.Masked("manifestCode") {
					rmap["manifestCode"] = rec.ManifestCode
				}
			case "ArrivedAt":
				if !row.Masked("arrivedAt") {
					rmap["arrivedAt"] = rec.ArrivedAt
				}
			}
		}

		return httpio.NewEncoder(w).Ok(rmap)
	})
}
