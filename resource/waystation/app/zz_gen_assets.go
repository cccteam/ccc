// Code generated by resourcegeneration. DO NOT EDIT.
// Source: pkg/resources

package app

import (
	"net/http"
	"time"

	"cloud.google.com/go/civil"
	"github.com/cccteam/ccc"
	"github.com/cccteam/ccc/accesstypes"
	"github.com/cccteam/ccc/resource"
	"github.com/cccteam/ccc/resource/waystation/pkg/resources"
	"github.com/cccteam/ccc/resource/waystation/pkg/router"
	"github.com/cccteam/ccc/tracer"
	"github.com/cccteam/httpio"
)

func (a *App) Assets() http.HandlerFunc {
	type asset struct {
		ID             ccc.UUID   `json:"id"             index:"true" perm:"-"`
		FacilityID     ccc.UUID   `json:"facilityId"     index:"true"`
		SerialNumber   string     `json:"serialNumber"   index:"true"`
		Name           string     `json:"name"`
		CommissionedOn civil.Date `json:"commissionedOn"`
		LastServicedAt *time.Time `json:"lastServicedAt"`
	}

	type response []map[string]any

	decoder := NewQueryDecoder[resources.Asset, asset](accesstypes.List)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewAssetQueryFromQuerySet(querySet)

		resp := response{}
		for row, err := range res.List(ctx, a.ResourceClient()) {
			if err != nil {
				return httpio.NewEncoder(w).ClientMessage(ctx, err)
			}
			rec := (*asset)(&row.Data)
			rmap := make(map[string]any)
			for _, field := range querySet.Fields() {
				switch string(field) {
				case "ID":
					if !row.Masked("id") {
						rmap["id"] = rec.ID
					}
				case "FacilityID":
					if !row.Masked("facilityId") {
						rmap["facilityId"] = rec.FacilityID
					}
				case "SerialNumber":
					if !row.Masked("serialNumber") {
						rmap["serialNumber"] = rec.SerialNumber
					}
				case "Name":
					if !row.Masked("name") {
						rmap["name"] = rec.Name
					}
				case "CommissionedOn":
					if !row.Masked("commissionedOn") {
						rmap["commissionedOn"] = rec.CommissionedOn
					}
				case "LastServicedAt":
					if !row.Masked("lastServicedAt") {
						rmap["lastServicedAt"] = rec.LastServicedAt
					}
				}
			}
			if capabilities := row.Capabilities(); capabilities != nil {
				rmap[resource.CapabilitiesProperty] = capabilities
			}
			resp = append(resp, rmap)
		}

		return httpio.NewEncoder(w).Ok(resp)
	})
}

func (a *App) Asset() http.HandlerFunc {
	type response struct {
		ID             ccc.UUID   `json:"id"             index:"true" perm:"-"`
		FacilityID     ccc.UUID   `json:"facilityId"`
		SerialNumber   string     `json:"serialNumber"   index:"true"`
		Name           string     `json:"name"`
		CommissionedOn civil.Date `json:"commissionedOn"`
		LastServicedAt *time.Time `json:"lastServicedAt"`
	}

	decoder := NewQueryDecoder[resources.Asset, response](accesstypes.Read)

	return httpio.Log(func(w http.ResponseWriter, r *http.Request) error {
		ctx, span := tracer.Start(r.Context())
		defer span.End()

		id := httpio.Param[ccc.UUID](r, router.AssetID)

		domain := httpio.Param[accesstypes.Domain](r, router.Domain)
		querySet, err := decoder.Decode(r, a.UserPermissions(r), accesstypes.DomainScope(domain))
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}

		res := resources.NewAssetQueryFromQuerySet(querySet).SetID(id)

		row, err := res.Read(ctx, a.ResourceClient())
		if err != nil {
			return httpio.NewEncoder(w).ClientMessage(ctx, err)
		}
		rec := (*response)(&row.Data)
		rmap := make(map[string]any)
		for _, field := range querySet.Fields() {
			switch string(field) {
			case "ID":
				if !row.Masked("id") {
					rmap["id"] = rec.ID
				}
			case "FacilityID":
				if !row.Masked("facilityId") {
					rmap["facilityId"] = rec.FacilityID
				}
			case "SerialNumber":
				if !row.Masked("serialNumber") {
					rmap["serialNumber"] = rec.SerialNumber
				}
			case "Name":
				if !row.Masked("name") {
					rmap["name"] = rec.Name
				}
			case "CommissionedOn":
				if !row.Masked("commissionedOn") {
					rmap["commissionedOn"] = rec.CommissionedOn
				}
			case "LastServicedAt":
				if !row.Masked("lastServicedAt") {
					rmap["lastServicedAt"] = rec.LastServicedAt
				}
			}
		}
		if capabilities := row.Capabilities(); capabilities != nil {
			rmap[resource.CapabilitiesProperty] = capabilities
		}

		return httpio.NewEncoder(w).Ok(rmap)
	})
}
