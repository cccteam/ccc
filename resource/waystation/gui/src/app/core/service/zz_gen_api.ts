// Code generated by resourcegeneration. DO NOT EDIT.
import { ApiDescriptor, Client, ClientOptions, createClient, MethodHandle, ResourceHandle } from '@cccteam/resource';
import { Methods, Resources } from './zz_gen_constants';
import { Assets, CatalogItems, Facilities, IncidentReports, InventoryLots, Modules, OpenWorkOrdersByTeams, Requisitions, RequisitionLines, Shipments, SpendByCategories, StaffMembers, Suppliers, Teams, TeamMemberships, Waystations, WorkOrders, WorkOrderTasks, FleetSummaries, StationStatusBoards } from './zz_gen_resources';
import { ApproveRequisition, CompleteWorkOrder, DeclineRequisition, NudgeWorkOrder, ReceiveShipment, RunSafetyDrill, ScheduleWorkOrder, StartWorkOrder, SubmitRequisition } from './zz_gen_methods';

/**
 * The fields a client may set when creating Assets. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface AssetsCreate {
  facilityId: string;
  serialNumber: string;
  name: string;
  commissionedOn: Date;
}
/** The fields a client may change on Assets. Keys, server-owned, and immutable fields are absent. */
export interface AssetsPatch {
  facilityId?: string;
  name?: string;
  commissionedOn?: Date;
}
/** The primary key of Assets, in route order. */
export type AssetsKey = [id: string];

/**
 * The fields a client may set when creating CatalogItems. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface CatalogItemsCreate {
  sku: string;
  name: string;
  categoryId: string;
  unitCost: number;
  hazardous: boolean;
}
/** The fields a client may change on CatalogItems. Keys, server-owned, and immutable fields are absent. */
export interface CatalogItemsPatch {
  name?: string;
  categoryId?: string;
  unitCost?: number;
  hazardous?: boolean;
}
/** The primary key of CatalogItems, in route order. */
export type CatalogItemsKey = [id: string];

/**
 * The fields a client may set when creating Facilities. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface FacilitiesCreate {
  moduleId: string;
  name: string;
  kind: string;
}
/** The fields a client may change on Facilities. Keys, server-owned, and immutable fields are absent. */
export interface FacilitiesPatch {
  moduleId?: string;
  name?: string;
  kind?: string;
}
/** The primary key of Facilities, in route order. */
export type FacilitiesKey = [id: string];

/**
 * The fields a client may set when creating IncidentReports. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface IncidentReportsCreate {
  summary: string;
  severity: number;
  reporterContact?: string;
  rawStatement?: string;
}
/** The fields a client may change on IncidentReports. Keys, server-owned, and immutable fields are absent. */
export interface IncidentReportsPatch {
  summary?: string;
  severity?: number;
  reporterContact?: string;
  rawStatement?: string;
}
/** The primary key of IncidentReports, in route order. */
export type IncidentReportsKey = [id: string];

/**
 * The fields a client may set when creating InventoryLots. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface InventoryLotsCreate {
  catalogItemId: string;
  quantity: number;
  expiresOn?: Date;
  binLocation: string;
}
/** The fields a client may change on InventoryLots. Keys, server-owned, and immutable fields are absent. */
export interface InventoryLotsPatch {
  catalogItemId?: string;
  quantity?: number;
  expiresOn?: Date;
  binLocation?: string;
}
/** The primary key of InventoryLots, in route order. */
export type InventoryLotsKey = [id: string];

/**
 * The fields a client may set when creating Modules. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface ModulesCreate {
  name: string;
  zone: string;
  pressureRated: boolean;
}
/** The fields a client may change on Modules. Keys, server-owned, and immutable fields are absent. */
export interface ModulesPatch {
  name?: string;
  zone?: string;
  pressureRated?: boolean;
}
/** The primary key of Modules, in route order. */
export type ModulesKey = [id: string];

/** The primary key of OpenWorkOrdersByTeams, in route order. */
export type OpenWorkOrdersByTeamsKey = [teamId: string];

/**
 * The fields a client may set when creating Requisitions. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface RequisitionsCreate {
  justification?: string;
  neededBy: Date;
}
/** The fields a client may change on Requisitions. Keys, server-owned, and immutable fields are absent. */
export interface RequisitionsPatch {
  justification?: string;
  neededBy?: Date;
}
/** The primary key of Requisitions, in route order. */
export type RequisitionsKey = [id: string];

/**
 * The fields a client may set when creating RequisitionLines. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface RequisitionLinesCreate {
  requisitionId: string;
  lineNumber: number;
  catalogItemId: string;
  quantity: number;
  unitCostSnapshot: number;
}
/** The fields a client may change on RequisitionLines. Keys, server-owned, and immutable fields are absent. */
export interface RequisitionLinesPatch {
  catalogItemId?: string;
  quantity?: number;
  unitCostSnapshot?: number;
}
/** The primary key of RequisitionLines, in route order. */
export type RequisitionLinesKey = [requisitionId: string, lineNumber: number];

/**
 * The fields a client may set when creating Shipments. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface ShipmentsCreate {
  supplierId: string;
  manifestCode: string;
  arrivedAt?: Date;
}
/** The fields a client may change on Shipments. Keys, server-owned, and immutable fields are absent. */
export interface ShipmentsPatch {
  supplierId?: string;
  arrivedAt?: Date;
}
/** The primary key of Shipments, in route order. */
export type ShipmentsKey = [id: string];

/** The primary key of SpendByCategories, in route order. */
export type SpendByCategoriesKey = [category: string];

/**
 * The fields a client may set when creating StaffMembers. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface StaffMembersCreate {
  userId: string;
  displayName: string;
  approvalLimit: number;
  homeWaystationId?: string;
}
/** The fields a client may change on StaffMembers. Keys, server-owned, and immutable fields are absent. */
export interface StaffMembersPatch {
  userId?: string;
  displayName?: string;
  approvalLimit?: number;
  homeWaystationId?: string;
}
/** The primary key of StaffMembers, in route order. */
export type StaffMembersKey = [id: string];

/**
 * The fields a client may set when creating Suppliers. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface SuppliersCreate {
  name: string;
  contactName: string;
  contactEmail: string;
  active: boolean;
}
/** The fields a client may change on Suppliers. Keys, server-owned, and immutable fields are absent. */
export interface SuppliersPatch {
  name?: string;
  contactName?: string;
  contactEmail?: string;
  active?: boolean;
}
/** The primary key of Suppliers, in route order. */
export type SuppliersKey = [id: string];

/**
 * The fields a client may set when creating Teams. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface TeamsCreate {
  name: string;
  specialty: string;
}
/** The fields a client may change on Teams. Keys, server-owned, and immutable fields are absent. */
export interface TeamsPatch {
  name?: string;
  specialty?: string;
}
/** The primary key of Teams, in route order. */
export type TeamsKey = [id: string];

/**
 * The fields a client may set when creating TeamMemberships. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface TeamMembershipsCreate {
  teamId: string;
  userId: string;
}
/** The fields a client may change on TeamMemberships. Keys, server-owned, and immutable fields are absent. */
export type TeamMembershipsPatch = Record<never, never>;
/** The primary key of TeamMemberships, in route order. */
export type TeamMembershipsKey = [teamId: string, userId: string];

/**
 * The fields a client may set when creating Waystations. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface WaystationsCreate {
  id: string;
  name: string;
  orbitBand: string;
  commissioned: Date;
}
/** The fields a client may change on Waystations. Keys, server-owned, and immutable fields are absent. */
export interface WaystationsPatch {
  name?: string;
  orbitBand?: string;
  commissioned?: Date;
}
/** The primary key of Waystations, in route order. */
export type WaystationsKey = [id: string];

/**
 * The fields a client may set when creating WorkOrders. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface WorkOrdersCreate {
  assetId: string;
  title: string;
  summary?: string;
  priority: number;
  assignedTeamId?: string;
  dueAt?: Date;
}
/** The fields a client may change on WorkOrders. Keys, server-owned, and immutable fields are absent. */
export interface WorkOrdersPatch {
  assetId?: string;
  title?: string;
  summary?: string;
  priority?: number;
  assignedTeamId?: string;
  dueAt?: Date;
}
/** The primary key of WorkOrders, in route order. */
export type WorkOrdersKey = [id: string];

/**
 * The fields a client may set when creating WorkOrderTasks. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface WorkOrderTasksCreate {
  workOrderId: string;
  taskNumber: number;
  instructions: string;
  done: boolean;
}
/** The fields a client may change on WorkOrderTasks. Keys, server-owned, and immutable fields are absent. */
export interface WorkOrderTasksPatch {
  instructions?: string;
  done?: boolean;
}
/** The primary key of WorkOrderTasks, in route order. */
export type WorkOrderTasksKey = [workOrderId: string, taskNumber: number];

/** The primary key of FleetSummaries, in route order. */
export type FleetSummariesKey = [waystationId: string];

/** The primary key of StationStatusBoards, in route order. */
export type StationStatusBoardsKey = [facilityId: string, metric: string];

/** The generated API as the @cccteam/resource runtime addresses it. Routes carry no API prefix. */
export const apiDescriptor: ApiDescriptor = {
  domainRoute: { segment: 'waystations', param: 'waystationID' },
  consolidatedRoute: 'resources',
  permissionDigestRoute: 'permission-digest',
  userDomainsRoute: 'user-domains',
  resources: {
    [Resources.Assets]: {
      resource: Resources.Assets,
      property: 'assets',
      route: 'assets',
      scope: 'domain',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['facilityId', 'name', 'commissionedOn'],
    },
    [Resources.CatalogItems]: {
      resource: Resources.CatalogItems,
      property: 'catalogItems',
      route: 'catalog-items',
      scope: 'global',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['name', 'categoryId', 'unitCost', 'hazardous'],
    },
    [Resources.Facilities]: {
      resource: Resources.Facilities,
      property: 'facilities',
      route: 'facilities',
      scope: 'domain',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['moduleId', 'name', 'kind'],
    },
    [Resources.IncidentReports]: {
      resource: Resources.IncidentReports,
      property: 'incidentReports',
      route: 'incident-reports',
      scope: 'domain',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['summary', 'severity', 'reporterContact', 'rawStatement'],
    },
    [Resources.InventoryLots]: {
      resource: Resources.InventoryLots,
      property: 'inventoryLots',
      route: 'inventory-lots',
      scope: 'domain',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['catalogItemId', 'quantity', 'expiresOn', 'binLocation'],
    },
    [Resources.Modules]: {
      resource: Resources.Modules,
      property: 'modules',
      route: 'modules',
      scope: 'domain',
      consolidated: false,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove'],
      patchable: ['name', 'zone', 'pressureRated'],
    },
    [Resources.OpenWorkOrdersByTeams]: {
      resource: Resources.OpenWorkOrdersByTeams,
      property: 'openWorkOrdersByTeams',
      route: 'open-work-orders-by-teams',
      scope: 'domain',
      consolidated: false,
      keys: ['teamId'],
      operations: ['list', 'read'],
    },
    [Resources.Requisitions]: {
      resource: Resources.Requisitions,
      property: 'requisitions',
      route: 'requisitions',
      scope: 'domain',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['justification', 'neededBy'],
    },
    [Resources.RequisitionLines]: {
      resource: Resources.RequisitionLines,
      property: 'requisitionLines',
      route: 'requisition-lines',
      scope: 'domain',
      consolidated: true,
      keys: ['requisitionId', 'lineNumber'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['catalogItemId', 'quantity', 'unitCostSnapshot'],
    },
    [Resources.Shipments]: {
      resource: Resources.Shipments,
      property: 'shipments',
      route: 'shipments',
      scope: 'domain',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['supplierId', 'arrivedAt'],
    },
    [Resources.SpendByCategories]: {
      resource: Resources.SpendByCategories,
      property: 'spendByCategories',
      route: 'spend-by-categories',
      scope: 'global',
      consolidated: false,
      keys: ['category'],
      operations: ['list', 'read'],
    },
    [Resources.StaffMembers]: {
      resource: Resources.StaffMembers,
      property: 'staffMembers',
      route: 'staff-members',
      scope: 'global',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['userId', 'displayName', 'approvalLimit', 'homeWaystationId'],
    },
    [Resources.Suppliers]: {
      resource: Resources.Suppliers,
      property: 'suppliers',
      route: 'suppliers',
      scope: 'global',
      consolidated: false,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove'],
      patchable: ['name', 'contactName', 'contactEmail', 'active'],
    },
    [Resources.Teams]: {
      resource: Resources.Teams,
      property: 'teams',
      route: 'teams',
      scope: 'domain',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['name', 'specialty'],
    },
    [Resources.TeamMemberships]: {
      resource: Resources.TeamMemberships,
      property: 'teamMemberships',
      route: 'team-memberships',
      scope: 'domain',
      consolidated: true,
      keys: ['teamId', 'userId'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: [],
    },
    [Resources.Waystations]: {
      resource: Resources.Waystations,
      property: 'waystations',
      route: 'waystations',
      scope: 'global',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['name', 'orbitBand', 'commissioned'],
    },
    [Resources.WorkOrders]: {
      resource: Resources.WorkOrders,
      property: 'workOrders',
      route: 'work-orders',
      scope: 'domain',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['assetId', 'title', 'summary', 'priority', 'assignedTeamId', 'dueAt'],
    },
    [Resources.WorkOrderTasks]: {
      resource: Resources.WorkOrderTasks,
      property: 'workOrderTasks',
      route: 'work-order-tasks',
      scope: 'domain',
      consolidated: true,
      keys: ['workOrderId', 'taskNumber'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['instructions', 'done'],
    },
    [Resources.FleetSummaries]: {
      resource: Resources.FleetSummaries,
      property: 'fleetSummaries',
      route: 'fleet-summaries',
      scope: 'global',
      consolidated: false,
      keys: ['waystationId'],
      operations: ['list'],
    },
    [Resources.StationStatusBoards]: {
      resource: Resources.StationStatusBoards,
      property: 'stationStatusBoards',
      route: 'station-status-boards',
      scope: 'domain',
      consolidated: false,
      keys: ['facilityId', 'metric'],
      operations: ['list', 'read'],
    },
  },
  methods: {
    [Methods.ApproveRequisition]: { method: Methods.ApproveRequisition, property: 'approveRequisition', route: 'approve-requisition', scope: 'domain' },
    [Methods.CompleteWorkOrder]: { method: Methods.CompleteWorkOrder, property: 'completeWorkOrder', route: 'complete-work-order', scope: 'domain' },
    [Methods.DeclineRequisition]: { method: Methods.DeclineRequisition, property: 'declineRequisition', route: 'decline-requisition', scope: 'domain' },
    [Methods.NudgeWorkOrder]: { method: Methods.NudgeWorkOrder, property: 'nudgeWorkOrder', route: 'nudge-work-order', scope: 'domain' },
    [Methods.ReceiveShipment]: { method: Methods.ReceiveShipment, property: 'receiveShipment', route: 'receive-shipment', scope: 'domain' },
    [Methods.RunSafetyDrill]: { method: Methods.RunSafetyDrill, property: 'runSafetyDrill', route: 'run-safety-drill', scope: 'global' },
    [Methods.ScheduleWorkOrder]: { method: Methods.ScheduleWorkOrder, property: 'scheduleWorkOrder', route: 'schedule-work-order', scope: 'domain' },
    [Methods.StartWorkOrder]: { method: Methods.StartWorkOrder, property: 'startWorkOrder', route: 'start-work-order', scope: 'domain' },
    [Methods.SubmitRequisition]: { method: Methods.SubmitRequisition, property: 'submitRequisition', route: 'submit-requisition', scope: 'domain' },
  },
};

/** Handles for the global scope, available on the client root. */
export interface GlobalApi {
  catalogItems: ResourceHandle<CatalogItems, CatalogItemsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', CatalogItemsCreate, CatalogItemsPatch>;
  spendByCategories: ResourceHandle<SpendByCategories, SpendByCategoriesKey, 'list' | 'read'>;
  staffMembers: ResourceHandle<StaffMembers, StaffMembersKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', StaffMembersCreate, StaffMembersPatch>;
  suppliers: ResourceHandle<Suppliers, SuppliersKey, 'list' | 'read' | 'create' | 'patch' | 'remove', SuppliersCreate, SuppliersPatch>;
  waystations: ResourceHandle<Waystations, WaystationsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', WaystationsCreate, WaystationsPatch>;
  fleetSummaries: ResourceHandle<FleetSummaries, FleetSummariesKey, 'list'>;
  runSafetyDrill: MethodHandle<RunSafetyDrill>;
}

/** Handles for one tenant partition, available on client.domain(...). */
export interface DomainApi {
  assets: ResourceHandle<Assets, AssetsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', AssetsCreate, AssetsPatch>;
  facilities: ResourceHandle<Facilities, FacilitiesKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', FacilitiesCreate, FacilitiesPatch>;
  incidentReports: ResourceHandle<IncidentReports, IncidentReportsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', IncidentReportsCreate, IncidentReportsPatch>;
  inventoryLots: ResourceHandle<InventoryLots, InventoryLotsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', InventoryLotsCreate, InventoryLotsPatch>;
  modules: ResourceHandle<Modules, ModulesKey, 'list' | 'read' | 'create' | 'patch' | 'remove', ModulesCreate, ModulesPatch>;
  openWorkOrdersByTeams: ResourceHandle<OpenWorkOrdersByTeams, OpenWorkOrdersByTeamsKey, 'list' | 'read'>;
  requisitions: ResourceHandle<Requisitions, RequisitionsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', RequisitionsCreate, RequisitionsPatch>;
  requisitionLines: ResourceHandle<RequisitionLines, RequisitionLinesKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', RequisitionLinesCreate, RequisitionLinesPatch>;
  shipments: ResourceHandle<Shipments, ShipmentsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', ShipmentsCreate, ShipmentsPatch>;
  teams: ResourceHandle<Teams, TeamsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', TeamsCreate, TeamsPatch>;
  teamMemberships: ResourceHandle<TeamMemberships, TeamMembershipsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', TeamMembershipsCreate, TeamMembershipsPatch>;
  workOrders: ResourceHandle<WorkOrders, WorkOrdersKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', WorkOrdersCreate, WorkOrdersPatch>;
  workOrderTasks: ResourceHandle<WorkOrderTasks, WorkOrderTasksKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', WorkOrderTasksCreate, WorkOrderTasksPatch>;
  stationStatusBoards: ResourceHandle<StationStatusBoards, StationStatusBoardsKey, 'list' | 'read'>;
  approveRequisition: MethodHandle<ApproveRequisition>;
  completeWorkOrder: MethodHandle<CompleteWorkOrder>;
  declineRequisition: MethodHandle<DeclineRequisition>;
  nudgeWorkOrder: MethodHandle<NudgeWorkOrder>;
  receiveShipment: MethodHandle<ReceiveShipment>;
  scheduleWorkOrder: MethodHandle<ScheduleWorkOrder>;
  startWorkOrder: MethodHandle<StartWorkOrder>;
  submitRequisition: MethodHandle<SubmitRequisition>;
}

export type Api = Client<GlobalApi, DomainApi>;

/** Creates the typed client. baseUrl is the API prefix (for example '/api'). */
export function createApi(options: ClientOptions): Api {
  return createClient<GlobalApi, DomainApi>(apiDescriptor, options);
}
