// Code generated by resourcegeneration. DO NOT EDIT.
import { ApiDescriptor, Client, ClientOptions, createClient, ResourceHandle } from '@cccteam/resource';
import { Resources } from './zz_gen_constants';
import { Announcements } from './zz_gen_resources';

/**
 * The fields a client may set when creating Announcements. Server-owned fields are
 * absent; a server-generated key is absent, a client-assigned key is required.
 */
export interface AnnouncementsCreate {
  title: string;
  body: string;
}
/** The fields a client may change on Announcements. Keys, server-owned, and immutable fields are absent. */
export interface AnnouncementsPatch {
  title?: string;
  body?: string;
}
/** The primary key of Announcements, in route order. */
export type AnnouncementsKey = [id: string];

/** The generated API as the @cccteam/resource runtime addresses it. Routes carry no API prefix. */
export const apiDescriptor: ApiDescriptor = {
  domainRoute: { segment: 'tenants', param: 'tenantID' },
  consolidatedRoute: 'resources',
  permissionDigestRoute: 'permission-digest',
  userDomainsRoute: 'user-domains',
  resources: {
    [Resources.Announcements]: {
      resource: Resources.Announcements,
      property: 'announcements',
      route: 'announcements',
      scope: 'domain',
      consolidated: true,
      keys: ['id'],
      operations: ['list', 'read', 'create', 'patch', 'remove', 'batch'],
      patchable: ['title', 'body'],
    },
  },
  methods: {
  },
};

/** Handles for the global scope, available on the client root. */
export interface GlobalApi {
}

/** Handles for one tenant partition, available on client.domain(...). */
export interface DomainApi {
  announcements: ResourceHandle<Announcements, AnnouncementsKey, 'list' | 'read' | 'create' | 'patch' | 'remove' | 'batch', AnnouncementsCreate, AnnouncementsPatch>;
}

export type Api = Client<GlobalApi, DomainApi>;

/** Creates the typed client. baseUrl is the API prefix (for example '/api'). */
export function createApi(options: ClientOptions): Api {
  return createClient<GlobalApi, DomainApi>(apiDescriptor, options);
}
