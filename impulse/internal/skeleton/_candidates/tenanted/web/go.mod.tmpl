// This file exists solely as a Go module boundary: it stops Go tooling
// (go build/test/vet ./..., golangci-lint) from descending into the Angular
// workspace's tree, where npm dependencies can ship stray Go files
// (e.g. flatted's golang port). The workspace holds the console; it is not a Go module in any real sense and contains
// no Go code.
module github.com/cccteam/ccc/impulse/internal/skeleton/_candidates/tenanted/web

go 1.26.6
