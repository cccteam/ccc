# Semgrep path exclusions for the accesstypes module scan (the repository root's
# .semgrepignore carries the same entry under its root-relative path).
#
# The property-based test, and the conditiontest generator it draws its cases from, use a
# seeded math/rand, which is what a reproducible property test needs; the crypto-randomness
# audit does not apply to them.
condition/property_test.go
condition/conditiontest/generator.go
condition/conditiontest/generator_test.go
condition/conditiontest/evaluate_test.go
